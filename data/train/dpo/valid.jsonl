{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\ndef _contains_tool_token(value: str, names: set[str]) -> bool:\n    \"\"\"Exact identifier-token lookup using the active schema, not protocol keywords.\"\"\"\n    lower = value.lower()\n    for name in names:\n        needle = name.lower()\n        cursor = 0\n        while True:\n            index = lower.find(needle, cursor)\n            if index < 0:\n                break\n            before = lower[index - 1] if index else \"\"\n            after_index = index + len(needle)\n            after = lower[after_index] if after_index < len(lower) else \"\"\n            if (not before or not (before.isalnum() or before == \"_\")) and (\n                not after or not (after.isalnum() or after == \"_\")\n            ):\n                return True\n            cursor = index + 1\n    return False\n\n```", "chosen": "```python\nimport pytest\nfrom solution import _contains_tool_token\n\ndef test_contains_tool_token_exact_match():\n    assert _contains_tool_token('test-tool', {'test-tool'}) is True\n\ndef test_contains_tool_token_partial_match():\n    assert _contains_tool_token('my-test-tool', {'test-tool'}) is True\n\ndef test_contains_tool_token_not_exact_boundary():\n    assert _contains_tool_token('tool', {'tool'}) is True\n\ndef test_contains_tool_token_no_boundary():\n    assert _contains_tool_token('tool', {'tool'}) is True\n\ndef test_contains_tool_token_no_match():\n    assert _contains_tool_token('test-tooling', {'test-tool'}) is False\n\ndef test_contains_tool_token_boundary_case_insensitive():\n    assert _contains_tool_token('Test-Tool', {'test-tool'}) is True\n\ndef test_contains_tool_token_boundary_empty():\n    assert _contains_tool_token('', {'test-tool'}) is False\n\ndef test_contains_tool_token_boundary_empty_set():\n    assert _contains_tool_token('test-tool', set()) is False\n```", "rejected": "```python\nimport pytest\nfrom solution import _contains_tool_token\n\ndef test_contains_tool_token_with_matching_tool():\n    assert _contains_tool_token('get_users', {'get'}) is True\n\ndef test_contains_tool_token_no_space_before_tool():\n    assert _contains_tool_token('get_users', {'get'}) is True\n\ndef test_contains_tool_token_no_space_after_tool():\n    assert _contains_tool_token('get_users', {'get'}) is True\n\ndef test_contains_tool_token_with_space_before_tool():\n    assert _contains_tool_token('get_users', {'get'}) is True\n\ndef test_contains_tool_token_with_space_after_tool():\n    assert _contains_tool_token('get_users', {'get'}) is True\n\ndef test_contains_tool_token_with_punctuation_before_tool():\n    assert _contains_tool_token('get/users', {'get'}) is True\n\ndef test_contains_tool_token_with_punctuation_after_tool():\n    assert _contains_tool_token('get/users', {'get'}) is True\n\ndef test_contains_tool_token_no_match():\n    assert _contains_tool_token('get_users', {'post'}) is False\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\ndef _contains_tool_token(value: str, names: set[str]) -> bool:\n    \"\"\"Exact identifier-token lookup using the active schema, not protocol keywords.\"\"\"\n    lower = value.lower()\n    for name in names:\n        needle = name.lower()\n        cursor = 0\n        while True:\n            index = lower.find(needle, cursor)\n            if index < 0:\n                break\n            before = lower[index - 1] if index else \"\"\n            after_index = index + len(needle)\n            after = lower[after_index] if after_index < len(lower) else \"\"\n            if (not before or not (before.isalnum() or before == \"_\")) and (\n                not after or not (after.isalnum() or after == \"_\")\n            ):\n                return True\n            cursor = index + 1\n    return False\n\n```", "chosen": "```python\nimport pytest\nfrom solution import _contains_tool_token\n\ndef test_empty_string_with_non_empty_names():\n    assert _contains_tool_token('', {'tool'}) is False\n\ndef test_empty_string_with_empty_names():\n    assert _contains_tool_token('', set()) is False\n\ndef test_empty_names_with_non_empty_value():\n    assert _contains_tool_token('hello', set()) is False\n\ndef test_value_with_tool_but_surrounded_by_alnum():\n    assert _contains_tool_token('tooling', {'tool'}) is False\n\ndef test_value_with_tool_but_surrounded_by_underscore():\n    assert _contains_tool_token('tooling', {'tool'}) is False\n\ndef test_value_with_tool_but_surrounded_by_digit():\n    assert _contains_tool_token('tool1', {'tool'}) is False\n\ndef test_value_with_tool_but_surrounded_by_space():\n    assert _contains_tool_token(' tool', {'tool'}) is True\n\ndef test_value_with_tool_but_surrounded_by_newline():\n    assert _contains_tool_token('\\ntool\\n', {'tool'}) is True\n```", "rejected": "```python\nimport pytest\nfrom solution import _contains_tool_token\n\ndef test_both_sides_non_alnum():\n    assert _contains_tool_token('tool-bar', {'bar'}) is True\n\ndef test_both_sides_alnum():\n    assert _contains_tool_token('tool_bar', {'bar'}) is False\n\ndef test_single_side_non_alnum():\n    assert _contains_tool_token('tool bar', {'bar'}) is True\n\ndef test_single_side_alnum():\n    assert _contains_tool_token('tool-bar', {'bar'}) is False\n\ndef test_empty_names():\n    assert _contains_tool_token('tool bar', set()) is False\n\ndef test_empty_value():\n    assert _contains_tool_token('', {'bar'}) is False\n\ndef test_no_match_in_value():\n    assert _contains_tool_token('tool', {'bar'}) is False\n\ndef test_case_insensitive():\n    assert _contains_tool_token('TOOL-bar', {'Bar'}) is True\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Literal\n\n\ndef _json_has_call_shape(parsed: Any) -> bool:\n    \"\"\"Recognize the function-call *data model*, independent of wrapper spellings.\"\"\"\n    if isinstance(parsed, list):\n        return any(_json_has_call_shape(item) for item in parsed)\n    if not isinstance(parsed, dict):\n        return False\n    arg_fields = {\"arguments\", \"args\", \"parameters\", \"input\", \"payload\"}\n    identity_fields = {\"name\", \"tool\", \"tool_name\"}\n    keys = {str(key) for key in parsed}\n    function = parsed.get(\"function\")\n    if isinstance(function, dict):\n        fn_keys = {str(key) for key in function}\n        if fn_keys & identity_fields and (fn_keys & arg_fields or keys & arg_fields):\n            return True\n    if keys & identity_fields and keys & arg_fields:\n        return True\n    return any(_json_has_call_shape(value) for value in parsed.values())\n\n```", "chosen": "```python\nfrom solution import _json_has_call_shape\nimport pytest\n\ndef test_empty_list():\n    assert _json_has_call_shape([]) is False\n\ndef test_nested_empty_dictionaries():\n    assert _json_has_call_shape({}) is False\n\ndef test_list_of_empty_dicts():\n    assert _json_has_call_shape([{}, {}, {}]) is False\n\ndef test_list_with_nested_empty_dicts():\n    assert _json_has_call_shape([{}, {'name': 'foo'}, {}]) is False\n\ndef test_list_with_valid_function_call():\n    data = [{'name': 'foo', 'arguments': {'arg1': 1}}]\n    assert _json_has_call_shape(data) is True\n\ndef test_dict_with_function_dict_and_arg_fields():\n    data = {'function': {'name': 'foo'}, 'arguments': {'arg1': 1}}\n    assert _json_has_call_shape(data) is True\n\ndef test_complex_nested_structure_with_mixed_content():\n    data = {'name': 'foo', 'arguments': {'arg1': 1}, 'some_field': {'other_field': 'value'}}\n    assert _json_has_call_shape(data) is True\n\ndef test_function_dict_with_identity_fields_but_no_args():\n    data = {'function': {'name': 'foo', 'tool': 'bar'}}\n    assert _json_has_call_shape(data) is False\n```", "rejected": "```python\nimport pytest\nfrom solution import _json_has_call_shape\n\ndef test_list_of_valid_calls():\n    data = [{'function': {'name': 'foo'}}, {'arguments': 1}]\n    assert _json_has_call_shape(data) is True\n\ndef test_nested_list_with_valid_call():\n    data = [{'arguments': []}, [{'function': {'name': 'bar'}}]]\n    assert _json_has_call_shape(data) is True\n\ndef test_empty_list():\n    assert _json_has_call_shape([]) is False\n\ndef test_empty_dict():\n    assert _json_has_call_shape({}) is False\n\ndef test_dict_only_identity_fields():\n    data = {'name': 'foo', 'tool': 'bar'}\n    assert _json_has_call_shape(data) is False\n\ndef test_dict_only_argument_fields():\n    data = {'arguments': [], 'parameters': []}\n    assert _json_has_call_shape(data) is False\n\ndef test_dict_with_function_but_no_args_and_no_identity():\n    data = {'function': {'name': 'foo'}}\n    assert _json_has_call_shape(data) is False\n\ndef test_dict_with_function_and_identity_and_arg_fields():\n    data = {'name': 'foo', 'arguments': []}\n    assert _json_has_call_shape(data) is True\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Literal\n\n\ndef _json_has_call_shape(parsed: Any) -> bool:\n    \"\"\"Recognize the function-call *data model*, independent of wrapper spellings.\"\"\"\n    if isinstance(parsed, list):\n        return any(_json_has_call_shape(item) for item in parsed)\n    if not isinstance(parsed, dict):\n        return False\n    arg_fields = {\"arguments\", \"args\", \"parameters\", \"input\", \"payload\"}\n    identity_fields = {\"name\", \"tool\", \"tool_name\"}\n    keys = {str(key) for key in parsed}\n    function = parsed.get(\"function\")\n    if isinstance(function, dict):\n        fn_keys = {str(key) for key in function}\n        if fn_keys & identity_fields and (fn_keys & arg_fields or keys & arg_fields):\n            return True\n    if keys & identity_fields and keys & arg_fields:\n        return True\n    return any(_json_has_call_shape(value) for value in parsed.values())\n\n```", "chosen": "```python\nimport pytest\nfrom solution import _json_has_call_shape\n\ndef test_list_with_calls():\n    data = {'a': {'function': {'name': 'test1'}, 'arguments': [1, 2]}}\n    assert _json_has_call_shape(data) is True\n\ndef test_list_without_calls():\n    data = {'a': {'name': 'test1'}}\n    assert _json_has_call_shape(data) is False\n\ndef test_nested_list_with_calls():\n    data = [{'function': {'name': 'test1'}, 'arguments': [1]}, {'name': 'test2'}]\n    assert _json_has_call_shape(data) is True\n\ndef test_nested_list_without_calls():\n    data = [{'name': 'test1'}, {'name': 'test2'}]\n    assert _json_has_call_shape(data) is False\n\ndef test_dict_with_function_dict_and_arg_fields():\n    data = {'function': {'name': 'test1'}, 'arguments': [1]}\n    assert _json_has_call_shape(data) is True\n\ndef test_dict_with_function_dict_and_identity_fields_only():\n    data = {'name': 'test1', 'tool': 'tool1'}\n    assert _json_has_call_shape(data) is False\n\ndef test_dict_with_identity_and_arg_fields():\n    data = {'name': 'test1', 'arguments': [1]}\n    assert _json_has_call_shape(data) is True\n\ndef test_dict_with_function_dict_and_function_arg_fields():\n    data = {'function': {'name': 'test1', 'arguments': [1]}}\n    assert _json_has_call_shape(data) is True\n```", "rejected": "```python\nfrom solution import _json_has_call_shape\n\ndef test_deep_list_with_matching_dict():\n    data = [{'function': {'name': 'test_fn', 'arguments': {'name': 'a'}}}]\n    assert _json_has_call_shape(data) is True\n\ndef test_dict_with_function_and_args():\n    data = {'function': {'name': 'test_fn', 'arguments': {'name': 'a'}}, 'tool': 'mytool'}\n    assert _json_has_call_shape(data) is True\n\ndef test_dict_without_identity_fields():\n    data = {'arguments': {'name': 'a'}, 'tool_name': 'mytool_name'}\n    assert _json_has_call_shape(data) is False\n\ndef test_function_dict_with_identity_and_args():\n    data = {'function': {'name': 'fn', 'arguments': {'name': 'a'}}, 'tool': 't'}\n    assert _json_has_call_shape(data) is True\n\ndef test_function_dict_with_identity_but_no_args():\n    data = {'function': {'name': 'fn', 'tool': 't'}}\n    assert _json_has_call_shape(data) is False\n\ndef test_nested_list_with_one_match():\n    data = [{'tool': 'x'}, {'function': {'name': 'fn', 'arguments': {'name': 'a'}}}]\n    assert _json_has_call_shape(data) is True\n\ndef test_empty_container():\n    assert _json_has_call_shape([]) is False\n    assert _json_has_call_shape({}) is False\n\ndef test_primitive_values():\n    assert _json_has_call_shape('string') is False\n    assert _json_has_call_shape(42) is False\n    assert _json_has_call_shape(None) is False\n    assert _json_has_call_shape({'name': 'x'}) is False\n    assert _json_has_call_shape({'arguments': {'name': 'a'}}) is False\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\ndef completion_scope_id(task_id: str, attempt_id: str) -> str:\n    \"\"\"Stable lifecycle scope shared by Runtime, projections, and UI.\"\"\"\n\n    task = str(task_id or \"\").strip()\n    attempt = str(attempt_id or \"\").strip()\n    return f\"task:{task}:attempt:{attempt}\" if task or attempt else \"\"\n\n```", "chosen": "```python\nfrom solution import completion_scope_id\n\ndef test_completion_scope_id_both_valid():\n    assert completion_scope_id('task1', 'attempt1') == 'task:task1:attempt:attempt1'\n\ndef test_completion_scope_id_empty_strings():\n    assert completion_scope_id('', '') == ''\n\ndef test_completion_scope_id_only_task_valid():\n    assert completion_scope_id('task1', '') == 'task:task1:attempt:'\n\ndef test_completion_scope_id_only_attempt_valid():\n    assert completion_scope_id('', 'attempt1') == 'task::attempt:attempt1'\n\ndef test_completion_scope_id_whitespace_only():\n    assert completion_scope_id('   ', '   ') == ''\n\ndef test_completion_scope_id_white_space_around_values():\n    assert completion_scope_id('  task1  ', '  attempt1  ') == 'task:task1:attempt:attempt1'\n\ndef test_completion_scope_id_mixed_whitespace_and_empty():\n    assert completion_scope_id('  task1', '   ') == 'task:task1:attempt:'\n    assert completion_scope_id('  task1', '') == 'task:task1:attempt:'\n```", "rejected": "```python\nimport pytest\nfrom solution import completion_scope_id\n\ndef test_both_non_empty():\n    assert completion_scope_id('task1', 'attempt1') == 'task:task1:attempt:attempt1'\n\ndef test_task_empty_attempt_non_empty():\n    assert completion_scope_id('', 'attempt1') == 'task:attempt:attempt1'\n\ndef test_task_non_empty_attempt_empty():\n    assert completion_scope_id('task1', '') == 'task:task1:attempt:'\n\ndef test_both_empty():\n    assert completion_scope_id('', '') == ''\n\ndef test_spaces_around_values():\n    assert completion_scope_id(' task ', ' attempt ') == 'task:task:attempt:attempt'\n\ndef test_mixed_case():\n    assert completion_scope_id('Task', 'ATTEMPT') == 'task:task:attempt:attempt'\n\ndef test_only_task():\n    assert completion_scope_id('only_task', '') == 'task:only_task:attempt:'\n\ndef test_only_attempt():\n    assert completion_scope_id('', 'only_attempt') == 'task:attempt:only_attempt'\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\ndef completion_scope_id(task_id: str, attempt_id: str) -> str:\n    \"\"\"Stable lifecycle scope shared by Runtime, projections, and UI.\"\"\"\n\n    task = str(task_id or \"\").strip()\n    attempt = str(attempt_id or \"\").strip()\n    return f\"task:{task}:attempt:{attempt}\" if task or attempt else \"\"\n\n```", "chosen": "```python\nimport pytest\nfrom solution import completion_scope_id\n\ndef test_completion_scope_id_both_valid():\n    assert completion_scope_id('task1', 'attempt1') == 'task:task1:attempt:attempt1'\n\ndef test_completion_scope_id_task_only():\n    assert completion_scope_id('task1', '') == 'task:task1:attempt:'\n\ndef test_completion_scope_id_attempt_only():\n    assert completion_scope_id('', 'attempt1') == 'task::attempt:attempt1'\n\ndef test_completion_scope_id_both_empty():\n    assert completion_scope_id('', '') == ''\n\ndef test_completion_scope_id_whitespace_task():\n    assert completion_scope_id('   task   ', '') == 'task:task:attempt:'\n\ndef test_completion_scope_id_whitespace_attempt():\n    assert completion_scope_id('', '   attempt   ') == 'task::attempt:attempt'\n\ndef test_completion_scope_id_whitespace_both():\n    assert completion_scope_id('  task  ', '  attempt  ') == 'task:task:attempt:attempt'\n\ndef test_completion_scope_id_empty_strings():\n    assert completion_scope_id('', '') == ''\n```", "rejected": "```python\nimport pytest\nfrom solution import completion_scope_id\n\ndef test_both_non_empty():\n    assert completion_scope_id('task1', 'attempt1') == 'task:task1:attempt:attempt1'\n\ndef test_task_empty_attempt_non_empty():\n    assert completion_scope_id('', 'attempt1') == 'task:attempt:attempt1'\n\ndef test_task_non_empty_attempt_empty():\n    assert completion_scope_id('task1', '') == 'task:task1:attempt:'\n\ndef test_both_empty():\n    assert completion_scope_id('', '') == ''\n\ndef test_task_with_spaces():\n    assert completion_scope_id('  task1  ', '  attempt1  ') == 'task:task1:attempt:attempt1'\n\ndef test_task_one_space():\n    assert completion_scope_id(' task ', 'attempt') == 'task:task:attempt:attempt'\n\ndef test_attempt_one_space():\n    assert completion_scope_id('task', ' attempt ') == 'task:task:attempt:attempt'\n\ndef test_numbers_and_symbols():\n    assert completion_scope_id('abc123', 'xyz_789') == 'task:abc123:attempt:xyz_789'\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Awaitable, Callable, Iterator, Mapping\n\n\ndef _turn_input(turn_result: dict[str, Any] | None) -> str:\n    \"\"\"Return the complete input captured by Engine, without snapshot clipping.\"\"\"\n\n    if not isinstance(turn_result, dict):\n        return \"\"\n    value = turn_result.get(\"_memory_input\")\n    if isinstance(value, str):\n        return value\n    if value is None:\n        return \"\"\n    return str(value)\n\n```", "chosen": "```python\nimport pytest\nfrom solution import _turn_input\n\ndef test_turn_input_valid_string():\n    assert _turn_input({'_memory_input': 'hello'}) == 'hello'\n\ndef test_turn_input_none_memory():\n    assert _turn_input({'_memory_input': None}) == ''\n\ndef test_turn_input_non_dict():\n    assert _turn_input(None) == ''\n    assert _turn_input(123) == ''\n    assert _turn_input([]) == ''\n    assert _turn_input({'key': 'value'}) == ''\n\ndef test_turn_input_non_string_value():\n    assert _turn_input({'_memory_input': 42}) == '42'\n    assert _turn_input({'_memory_input': 3.14}) == '3.14'\n    assert _turn_input({'_memory_input': [1, 2, 3]}) == '[1, 2, 3]'\n    assert _turn_input({'_memory_input': {'a': 1}}) == \"{'a': 1}\"\n\ndef test_turn_input_empty_dict():\n    assert _turn_input({}) == ''\n\ndef test_turn_input_with_other_keys():\n    assert _turn_input({'_memory_input': 'test', 'extra': 'data'}) == 'test'\n\ndef test_turn_input_none_memory_input_key():\n    assert _turn_input({'_memory_input': None}) == ''\n```", "rejected": "```python\nimport pytest\nfrom solution import _turn_input\n\ndef test_empty_dict():\n    \"\"\"Test with an empty dictionary.\"\"\"\n    assert _turn_input({}) == ''\n\ndef test_non_dict_input():\n    \"\"\"Test with non-dict input (e.g., list, str, int).\"\"\"\n    assert _turn_input('string') == ''\n    assert _turn_input(123) == ''\n    assert _turn_input([1, 2, 3]) == ''\n\ndef test_memory_input_str():\n    \"\"\"Test with a valid string in _memory_input.\"\"\"\n    assert _turn_input({'_memory_input': 'hello world'}) == 'hello world'\n\ndef test_memory_input_none():\n    \"\"\"Test with None in _memory_input.\"\"\"\n    assert _turn_input({'_memory_input': None}) == ''\n\ndef test_memory_input_other_type():\n    \"\"\"Test with a non-string, non-None value in _memory_input.\"\"\"\n    value = _turn_input({'_memory_input': 123})\n    assert value == '123'\n\ndef test_memory_input_missing():\n    \"\"\"Test when _memory_input key is missing.\"\"\"\n    assert _turn_input({'other_key': 'value'}) == ''\n\ndef test_special_characters_in_string():\n    \"\"\"Test string with special characters.\"\"\"\n    assert _turn_input({'_memory_input': 'a\"b\\\\tc\\\\n'}) == 'a\"b\\t\\nc\\n'\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Awaitable, Callable, Iterator, Mapping\n\n\ndef _turn_input(turn_result: dict[str, Any] | None) -> str:\n    \"\"\"Return the complete input captured by Engine, without snapshot clipping.\"\"\"\n\n    if not isinstance(turn_result, dict):\n        return \"\"\n    value = turn_result.get(\"_memory_input\")\n    if isinstance(value, str):\n        return value\n    if value is None:\n        return \"\"\n    return str(value)\n\n```", "chosen": "```python\nfrom solution import _turn_input\nimport pytest\n\ndef test_normal_dict_with_string_memory():\n    assert _turn_input({'_memory_input': 'hello'}) == 'hello'\n\ndef test_dict_with_none_memory():\n    assert _turn_input({'_memory_input': None}) == ''\n\ndef test_dict_without_memory_key():\n    assert _turn_input({'other': 'data'}) == ''\n\ndef test_non_dict_input():\n    assert _turn_input(None) == ''\n    assert _turn_input(123) == ''\n    assert _turn_input([1, 2, 3]) == ''\n    assert _turn_input({'key': 'value'}) == ''\n\ndef test_dict_with_non_string_non_none_memory():\n    assert _turn_input({'_memory_input': 123}) == '123'\n    assert _turn_input({'_memory_input': []}) == '[]'\n    assert _turn_input({'_memory_input': {}}) == '{}'\n    assert _turn_input({'_memory_input': 0.5}) == '0.5'\n\ndef test_empty_string_input():\n    assert _turn_input('') == ''\n\ndef test_empty_dict_input():\n    assert _turn_input({}) == ''\n```", "rejected": "```python\nimport pytest\nfrom solution import _turn_input\n\ndef test_normal_string_input():\n    result = _turn_input({'_memory_input': 'hello'})\n    assert result == 'hello'\n\ndef test_none_value():\n    result = _turn_input({'_memory_input': None})\n    assert result == ''\n\ndef test_non_dict_input():\n    result = _turn_input('not a dict')\n    assert result == ''\n\ndef test_empty_list_input():\n    result = _turn_input({'_memory_input': []})\n    assert result == ''\n\ndef test_non_string_value_in_dict():\n    result = _turn_input({'_memory_input': 123})\n    assert result == '123'\n\ndef test_dict_without_memory_input():\n    result = _turn_input({})\n    assert result == ''\n\ndef test_none_turn_result():\n    result = _turn_input(None)\n    assert result == ''\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Iterable\n\n\ndef notification_from_unknown(value: Any) -> dict[str, Any] | None:\n    \"\"\"Strictly normalize the internal completion-review notification boundary.\"\"\"\n\n    if not isinstance(value, dict) or value.get(\"kind\") != \"completion_review\":\n        return None\n    completion_id = str(value.get(\"completion_id\") or \"\").strip()\n    if not completion_id:\n        return None\n    allowed = {\"accept\", \"rework\", \"pause\", \"complete\", \"terminate\", \"retry\", \"reject\"}\n    decisions = [\n        str(item).strip().lower()\n        for item in value.get(\"decision_required\", [])\n        if str(item).strip().lower() in allowed\n    ] if isinstance(value.get(\"decision_required\"), list) else []\n    return {\n        \"kind\": \"completion_review\",\n        \"completion_id\": completion_id,\n        \"report_ref\": str(value.get(\"report_ref\") or \"\").strip(),\n        \"decision_required\": decisions or [\"accept\", \"rework\"],\n    }\n\n```", "chosen": "```python\nfrom solution import notification_from_unknown\n\ndef test_valid_input_normalizes_decently():\n    value = {'kind': 'completion_review', 'completion_id': 'abc-123', 'report_ref': 'ref-456', 'decision_required': ['Accept', 'REWORK', 'Pause']}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['kind'] == 'completion_review'\n    assert result['completion_id'] == 'abc-123'\n    assert result['report_ref'] == 'ref-456'\n    assert result['decision_required'] == ['accept', 'rework', 'pause']\n\ndef test_missing_kind_returns_none():\n    value = {'kind': 'other'}\n    assert notification_from_unknown(value) is None\n\ndef test_missing_completion_id_returns_none():\n    value = {'kind': 'completion_review', 'completion_id': None}\n    assert notification_from_unknown(value) is None\n\ndef test_empty_completion_id_returns_none():\n    value = {'kind': 'completion_review', 'completion_id': ''}\n    assert notification_from_unknown(value) is None\n\ndef test_whitespace_only_completion_id_returns_none():\n    value = {'kind': 'completion_review', 'completion_id': '   '}\n    assert notification_from_unknown(value) is None\n\ndef test_unknown_decision_excluded():\n    value = {'kind': 'completion_review', 'completion_id': 'abc-123', 'report_ref': 'ref-456', 'decision_required': ['Accept', 'InvalidDecision']}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['decision_required'] == ['accept']\n\ndef test_empty_decision_required_list_reverts_to_default():\n    value = {'kind': 'completion_review', 'completion_id': 'abc-123', 'report_ref': 'ref-456', 'decision_required': []}\n    result = notification_from_unknown(value)\n    assert result['decision_required'] == ['accept', 'rework']\n\ndef test_non_list_decision_required_returns_default():\n    value = {'kind': 'completion_review', 'completion_id': 'abc-123', 'report_ref': 'ref-456', 'decision_required': 'Accept'}\n    result = notification_from_unknown(value)\n    assert result['decision_required'] == ['accept', 'rework']\n```", "rejected": "```python\nimport pytest\nfrom solution import notification_from_unknown\n\ndef test_valid_input_with_all_fields():\n    value = {'kind': 'completion_review', 'completion_id': 'abc123', 'report_ref': 'ref456', 'decision_required': ['Accept', 'Rework']}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['kind'] == 'completion_review'\n    assert result['completion_id'] == 'abc123'\n    assert result['report_ref'] == 'ref456'\n    assert result['decision_required'] == ['accept', 'rework']\n\ndef test_missing_kind():\n    value = {'kind': 'other_type', 'completion_id': 'abc123'}\n    result = notification_from_unknown(value)\n    assert result is None\n\ndef test_missing_completion_id():\n    value = {'kind': 'completion_review', 'report_ref': 'ref123'}\n    result = notification_from_unknown(value)\n    assert result is None\n\ndef test_empty_completion_id():\n    value = {'kind': 'completion_review', 'completion_id': '', 'report_ref': 'ref123'}\n    result = notification_from_unknown(value)\n    assert result is None\n\ndef test_completion_id_with_spaces():\n    value = {'kind': 'completion_review', 'completion_id': '  trimmed  ', 'report_ref': 'ref123'}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['completion_id'] == 'trimmed'\n\ndef test_decision_required_not_a_list():\n    value = {'kind': 'completion_review', 'completion_id': 'abc123', 'report_ref': 'ref123', 'decision_required': 'invalid'}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['decision_required'] == ['accept', 'rework']\n\ndef test_decision_required_with_invalid_options():\n    value = {'kind': 'completion_review', 'completion_id': 'abc123', 'report_ref': 'ref123', 'decision_required': ['Accept', 'InvalidOption']}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['decision_required'] == ['accept', 'rework']\n\ndef test_decision_required_with_all_valid_options():\n    value = {'kind': 'completion_review', 'completion_id': 'abc123', 'report_ref': 'ref123', 'decision_required': ['Accept', 'Rework', 'Pause', 'Complete', 'Terminate', 'Retry', 'Reject']}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['decision_required'] == ['accept', 'rework', 'pause', 'complete', 'terminate', 'retry', 'reject']\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Iterable\n\n\ndef notification_from_unknown(value: Any) -> dict[str, Any] | None:\n    \"\"\"Strictly normalize the internal completion-review notification boundary.\"\"\"\n\n    if not isinstance(value, dict) or value.get(\"kind\") != \"completion_review\":\n        return None\n    completion_id = str(value.get(\"completion_id\") or \"\").strip()\n    if not completion_id:\n        return None\n    allowed = {\"accept\", \"rework\", \"pause\", \"complete\", \"terminate\", \"retry\", \"reject\"}\n    decisions = [\n        str(item).strip().lower()\n        for item in value.get(\"decision_required\", [])\n        if str(item).strip().lower() in allowed\n    ] if isinstance(value.get(\"decision_required\"), list) else []\n    return {\n        \"kind\": \"completion_review\",\n        \"completion_id\": completion_id,\n        \"report_ref\": str(value.get(\"report_ref\") or \"\").strip(),\n        \"decision_required\": decisions or [\"accept\", \"rework\"],\n    }\n\n```", "chosen": "```python\nfrom solution import notification_from_unknown\n\ndef test_valid_completion_review():\n    data = {'kind': 'completion_review', 'completion_id': 'abc123', 'decision_required': ['accept', 'rework'], 'report_ref': 'ref-456'}\n    result = notification_from_unknown(data)\n    assert result is not None\n    assert result['kind'] == 'completion_review'\n    assert result['completion_id'] == 'abc123'\n    assert result['report_ref'] == 'ref-456'\n    assert result['decision_required'] == ['accept', 'rework']\n\ndef test_missing_kind():\n    data = {'other': 'value'}\n    assert notification_from_unknown(data) is None\n\ndef test_invalid_completion_id():\n    data = {'kind': 'completion_review', 'completion_id': None, 'decision_required': ['accept']}\n    assert notification_from_unknown(data) is None\n\ndef test_empty_completion_id():\n    data = {'kind': 'completion_review', 'completion_id': '', 'decision_required': ['accept']}\n    assert notification_from_unknown(data) is None\n\ndef test_whitespace_completion_id():\n    data = {'kind': 'completion_review', 'completion_id': '   ', 'decision_required': ['accept']}\n    assert notification_from_unknown(data) is None\n\ndef test_invalid_decision():\n    data = {'kind': 'completion_review', 'completion_id': 'xyz', 'decision_required': ['invalid', 'accept']}\n    result = notification_from_unknown(data)\n    assert result is not None\n    assert result['decision_required'] == ['accept']\n\ndef test_decision_required_not_list():\n    data = {'kind': 'completion_review', 'completion_id': 'xyz', 'decision_required': 'maybe'}\n    result = notification_from_unknown(data)\n    assert result is not None\n    assert result['decision_required'] == ['accept', 'rework']\n\ndef test_empty_decision_required_list():\n    data = {'kind': 'completion_review', 'completion_id': 'xyz', 'decision_required': []}\n    result = notification_from_unknown(data)\n    assert result is not None\n    assert result['decision_required'] == ['accept', 'rework']\n```", "rejected": "```python\nimport pytest\nfrom solution import notification_from_unknown\n\ndef test_valid_completion_review():\n    value = {'kind': 'completion_review', 'completion_id': 'C123', 'report_ref': 'R456', 'decision_required': ['accept', 'rework']}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['kind'] == 'completion_review'\n    assert result['completion_id'] == 'C123'\n    assert result['report_ref'] == 'R456'\n    assert result['decision_required'] == ['accept', 'rework']\n\ndef test_invalid_kind():\n    value = {'kind': 'other', 'completion_id': 'C123'}\n    result = notification_from_unknown(value)\n    assert result is None\n\ndef test_missing_kind():\n    value = {'completion_id': 'C123'}\n    result = notification_from_unknown(value)\n    assert result is None\n\ndef test_empty_completion_id():\n    value = {'kind': 'completion_review', 'completion_id': ''}\n    result = notification_from_unknown(value)\n    assert result is None\n\ndef test_whitespace_completion_id():\n    value = {'kind': 'completion_review', 'completion_id': '   '}\n    result = notification_from_unknown(value)\n    assert result is None\n\ndef test_none_completion_id_value():\n    value = {'kind': 'completion_review', 'completion_id': None}\n    result = notification_from_unknown(value)\n    assert result is None\n\ndef test_invalid_decision():\n    value = {'kind': 'completion_review', 'completion_id': 'C123', 'report_ref': 'R456', 'decision_required': ['accept', 'invalid_decision']}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['decision_required'] == ['accept', 'rework']\n\ndef test_all_valid_decisions():\n    value = {'kind': 'completion_review', 'completion_id': 'C123', 'report_ref': 'R456', 'decision_required': ['accept', 'rework', 'pause', 'complete', 'terminate', 'retry', 'reject']}\n    result = notification_from_unknown(value)\n    assert result is not None\n    assert result['decision_required'] == ['accept', 'rework', 'pause', 'complete', 'terminate', 'retry', 'reject']\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Awaitable, Callable, Iterator, Mapping\n\n\ndef summarize_actions(actions: list[dict[str, Any]]) -> dict[str, Any]:\n    \"\"\"Audit-only aggregate; it deliberately makes no completion judgement.\"\"\"\n\n    return {\n        \"calls\": len(actions),\n        \"succeeded\": sum(1 for item in actions if item.get(\"ok\") is True),\n        \"failed\": sum(1 for item in actions if item.get(\"ok\") is False),\n        \"tools\": [str(item.get(\"name\") or \"\") for item in actions],\n        \"duration_ms\": sum(int(item.get(\"duration_ms\") or 0) for item in actions),\n    }\n\n```", "chosen": "```python\nfrom solution import summarize_actions\n\ndef test_empty_actions():\n    assert summarize_actions([]) == {'calls': 0, 'succeeded': 0, 'failed': 0, 'tools': [], 'duration_ms': 0}\n\ndef test_single_success():\n    assert summarize_actions([{'ok': True}]) == {'calls': 1, 'succeeded': 1, 'failed': 0, 'tools': [''], 'duration_ms': 0}\n\ndef test_single_failure():\n    assert summarize_actions([{'ok': False}]) == {'calls': 1, 'succeeded': 0, 'failed': 1, 'tools': [''], 'duration_ms': 0}\n\ndef test_multiple_mixed():\n    actions = [{'ok': True, 'name': 'tool_a', 'duration_ms': 10}, {'ok': False, 'name': 'tool_b', 'duration_ms': 20}, {'ok': True, 'name': 'tool_a', 'duration_ms': 30}]\n    expected = {'calls': 3, 'succeeded': 2, 'failed': 1, 'tools': ['tool_a', 'tool_b', 'tool_a'], 'duration_ms': 60}\n    assert summarize_actions(actions) == expected\n\ndef test_missing_optional_fields():\n    actions = [{'duration_ms': None}]\n    assert summarize_actions(actions) == {'calls': 1, 'succeeded': 0, 'failed': 0, 'tools': [''], 'duration_ms': 0}\n\ndef test_duration_ms_zero_explicit():\n    assert summarize_actions([{'duration_ms': 0}]) == {'calls': 1, 'succeeded': 0, 'failed': 0, 'tools': [''], 'duration_ms': 0}\n\ndef test_negative_duration_ms():\n    actions = [{'duration_ms': -5}]\n    result = summarize_actions(actions)\n    assert result['duration_ms'] == -5\n    assert result['calls'] == 1\n    assert result['succeeded'] == 0\n    assert result['failed'] == 0\n    assert result['tools'] == ['']\n```", "rejected": "```python\nfrom solution import summarize_actions\n\ndef test_empty_actions_list():\n    result = summarize_actions([])\n    assert result == {'calls': 0, 'succeeded': 0, 'failed': 0, 'tools': [], 'duration_ms': 0}\n\ndef test_single_success():\n    result = summarize_actions([{'ok': True}])\n    assert result['calls'] == 1\n    assert result['succeeded'] == 1\n    assert result['failed'] == 0\n    assert result['tools'] == ['']\n    assert result['duration_ms'] == 0\n\ndef test_single_failure():\n    result = summarize_actions([{'ok': False}])\n    assert result['calls'] == 1\n    assert result['succeeded'] == 0\n    assert result['failed'] == 1\n    assert result['tools'] == ['']\n    assert result['duration_ms'] == 0\n\ndef test_mixed_with_strings_and_duration():\n    result = summarize_actions([{'ok': True, 'name': 'action1', 'duration_ms': 100}, {'ok': False, 'name': 'action2', 'duration_ms': 50}, {'ok': True, 'name': 'action3', 'duration_ms': 200}])\n    assert result['calls'] == 3\n    assert result['succeeded'] == 2\n    assert result['failed'] == 1\n    assert result['tools'] == ['action1', 'action2', 'action3']\n    assert result['duration_ms'] == 350\n\ndef test_missing_optional_fields():\n    result = summarize_actions([{'ok': True}, {'ok': False, 'name': 'explicit_name'}, {'name': 'only_name', 'duration_ms': 30}])\n    assert result['calls'] == 3\n    assert result['succeeded'] == 1\n    assert result['failed'] == 2\n    assert result['tools'] == ['', 'explicit_name', 'only_name']\n    assert result['duration_ms'] == 30\n\ndef test_negative_or_zero_duration():\n    result = summarize_actions([{'ok': True, 'duration_ms': -50}, {'ok': True, 'duration_ms': 0}])\n    assert result['duration_ms'] == -50\n\ndef test_all_fields_none():\n    result = summarize_actions([{'name': None, 'duration_ms': None, 'ok': None}])\n    assert result['calls'] == 1\n    assert result['succeeded'] == 0\n    assert result['failed'] == 0\n    assert result['tools'] == ['']\n    assert result['duration_ms'] == 0\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Awaitable, Callable, Iterator, Mapping\n\n\ndef summarize_actions(actions: list[dict[str, Any]]) -> dict[str, Any]:\n    \"\"\"Audit-only aggregate; it deliberately makes no completion judgement.\"\"\"\n\n    return {\n        \"calls\": len(actions),\n        \"succeeded\": sum(1 for item in actions if item.get(\"ok\") is True),\n        \"failed\": sum(1 for item in actions if item.get(\"ok\") is False),\n        \"tools\": [str(item.get(\"name\") or \"\") for item in actions],\n        \"duration_ms\": sum(int(item.get(\"duration_ms\") or 0) for item in actions),\n    }\n\n```", "chosen": "```python\nfrom solution import summarize_actions\n\ndef test_empty_actions():\n    result = summarize_actions([])\n    assert result == {'calls': 0, 'succeeded': 0, 'failed': 0, 'tools': [], 'duration_ms': 0}\n\ndef test_single_action_ok():\n    result = summarize_actions([{'ok': True}])\n    assert result == {'calls': 1, 'succeeded': 1, 'failed': 0, 'tools': [''], 'duration_ms': 0}\n\ndef test_single_action_failed():\n    result = summarize_actions([{'ok': False}])\n    assert result == {'calls': 1, 'succeeded': 0, 'failed': 1, 'tools': [''], 'duration_ms': 0}\n\ndef test_multiple_actions_mixed():\n    actions = [{'ok': True, 'name': 'T1', 'duration_ms': 100}, {'ok': False, 'name': 'T2', 'duration_ms': 200}, {'ok': True, 'name': 'T3', 'duration_ms': 50}]\n    result = summarize_actions(actions)\n    assert result['calls'] == 3\n    assert result['succeeded'] == 2\n    assert result['failed'] == 1\n    assert result['tools'] == ['T1', 'T2', 'T3']\n    assert result['duration_ms'] == 350\n\ndef test_action_without_name():\n    actions = [{'ok': True}, {'ok': False, 'name': 'NamedTool'}]\n    result = summarize_actions(actions)\n    assert result['tools'] == ['', 'NamedTool']\n\ndef test_action_without_duration_ms():\n    actions = [{'ok': True, 'duration_ms': 100}, {'ok': False, 'duration_ms': 50}, {'ok': True}]\n    result = summarize_actions(actions)\n    assert result['duration_ms'] == 150\n\ndef test_negative_duration_ms():\n    actions = [{'ok': True, 'duration_ms': -50}, {'ok': False, 'duration_ms': 100}]\n    result = summarize_actions(actions)\n    assert result['duration_ms'] == 50\n\ndef test_ok_as_other_falsy():\n    actions = [{'ok': False}, {'ok': 'False'}, {'ok': None}, {'ok': 0}, {'ok': ''}]\n    result = summarize_actions(actions)\n    assert result['failed'] == 1\n    assert result['succeeded'] == 0\n```", "rejected": "```python\nfrom solution import summarize_actions\n\ndef test_empty_actions():\n    assert summarize_actions([]) == {'calls': 0, 'succeeded': 0, 'failed': 0, 'tools': [], 'duration_ms': 0}\n\ndef test_single_succeeded_action():\n    assert summarize_actions([{'ok': True, 'name': 'tool1', 'duration_ms': 100}]) == {'calls': 1, 'succeeded': 1, 'failed': 0, 'tools': ['tool1'], 'duration_ms': 100}\n\ndef test_single_failed_action():\n    assert summarize_actions([{'ok': False, 'name': 'tool2', 'duration_ms': 200}]) == {'calls': 1, 'succeeded': 0, 'failed': 1, 'tools': ['tool2'], 'duration_ms': 200}\n\ndef test_mixed_actions():\n    assert summarize_actions([{'ok': True, 'name': 'toolA', 'duration_ms': 50}, {'ok': False, 'name': 'toolB', 'duration_ms': 60}, {'ok': True, 'name': 'toolC', 'duration_ms': 30}]) == {'calls': 3, 'succeeded': 2, 'failed': 1, 'tools': ['toolA', 'toolB', 'toolC'], 'duration_ms': 140}\n\ndef test_missing_ok_field():\n    assert summarize_actions([{'name': 'toolX', 'duration_ms': 10}]) == {'calls': 1, 'succeeded': 0, 'failed': 1, 'tools': ['toolX'], 'duration_ms': 10}\n\ndef test_missing_name_field():\n    assert summarize_actions([{'ok': True, 'duration_ms': 20}]) == {'calls': 1, 'succeeded': 1, 'failed': 0, 'tools': [''], 'duration_ms': 20}\n\ndef test_missing_duration_ms_field():\n    assert summarize_actions([{'ok': True, 'name': 'toolY'}]) == {'calls': 1, 'succeeded': 1, 'failed': 0, 'tools': ['toolY'], 'duration_ms': 0}\n\ndef test_zero_duration_ms():\n    assert summarize_actions([{'ok': True, 'name': 'toolZ', 'duration_ms': 0}]) == {'calls': 1, 'succeeded': 1, 'failed': 0, 'tools': ['toolZ'], 'duration_ms': 0}\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Callable\nimport hashlib\n\n\ndef _prompt_meta(text: str | None) -> dict[str, Any]:\n    \"\"\"Return ``{prompt_len, prompt_sha256}`` for a contract objective.\"\"\"\n    if text is None:\n        return {\"prompt_len\": 0, \"prompt_sha256\": \"\"}\n    data = text.encode(\"utf-8\", errors=\"replace\")\n    return {\n        \"prompt_len\": len(data),\n        \"prompt_sha256\": hashlib.sha256(data).hexdigest(),\n    }\n\n```", "chosen": "```python\nfrom solution import _prompt_meta\nimport hashlib\n\ndef test_null_input():\n    result = _prompt_meta(None)\n    assert result == {'prompt_len': 0, 'prompt_sha256': ''}\n\ndef test_empty_string():\n    result = _prompt_meta('')\n    assert result == {'prompt_len': 0, 'prompt_sha256': hashlib.sha256(b'').hexdigest()}\n\ndef test_single_char():\n    result = _prompt_meta('a')\n    assert result['prompt_len'] == 1\n    assert result['prompt_sha256'] == hashlib.sha256(b'a').hexdigest()\n\ndef test_multi_char_utf8():\n    text = 'Hello, World!'\n    result = _prompt_meta(text)\n    assert result['prompt_len'] == len(text.encode('utf-8'))\n    assert result['prompt_sha256'] == hashlib.sha256(text.encode('utf-8')).hexdigest()\n\ndef test_unicode_characters():\n    text = '\u65e5\u672c\u8a9e'\n    result = _prompt_meta(text)\n    assert result['prompt_len'] > 0\n    assert result['prompt_sha256'] == hashlib.sha256(text.encode('utf-8')).hexdigest()\n\ndef test_non_ascii_with_special_characters():\n    text = 'caf\u00e9'\n    result = _prompt_meta(text)\n    assert result['prompt_len'] == 5\n    assert result['prompt_sha256'] == hashlib.sha256(text.encode('utf-8', errors='replace')).hexdigest()\n\ndef test_content_hash_matches_expected():\n    text = 'test data'\n    expected_sha = hashlib.sha256(text.encode('utf-8')).hexdigest()\n    result = _prompt_meta(text)\n    assert result['prompt_len'] == len(text.encode('utf-8'))\n    assert result['prompt_sha256'] == expected_sha\n```", "rejected": "```python\nimport pytest\nfrom solution import _prompt_meta\n\ndef test_valid_text():\n    result = _prompt_meta('hello world')\n    assert result['prompt_len'] == 11\n    assert len(result['prompt_sha256']) == 64\n    expected_sha = hashlib.sha256(b'hello world').hexdigest()\n    assert result['prompt_sha256'] == expected_sha\n\ndef test_empty_string():\n    result = _prompt_meta('')\n    assert result['prompt_len'] == 0\n    assert result['prompt_sha256'] == hashlib.sha256(b'').hexdigest()\n\ndef test_none_input():\n    result = _prompt_meta(None)\n    assert result['prompt_len'] == 0\n    assert result['prompt_sha256'] == ''\n\ndef test_unicode_characters():\n    result = _prompt_meta('\u4f60\u597d\u4e16\u754c')\n    assert result['prompt_len'] == len('\u4f60\u597d\u4e16\u754c'.encode('utf-8', errors='replace'))\n    assert len(result['prompt_sha256']) == 64\n\ndef test_special_characters():\n    result = _prompt_meta('!@#$%^&*()')\n    assert len(result['prompt_sha256']) == 64\n    assert result['prompt_len'] == len('!@#$%^&*()'.encode('utf-8'))\n\ndef test_very_long_text():\n    long_text = 'a' * 10000\n    result = _prompt_meta(long_text)\n    assert result['prompt_len'] == len(long_text.encode('utf-8'))\n    assert len(result['prompt_sha256']) == 64\n\ndef test_single_character():\n    result = _prompt_meta('x')\n    assert result['prompt_len'] == 1\n    assert len(result['prompt_sha256']) == 64\n\ndef test_bytes_with_replacement():\n    text = '\\x80'.replace('\\x80', 'a')\n    result = _prompt_meta(text)\n    assert result['prompt_len'] >= 1\n    assert len(result['prompt_sha256']) == 64\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Callable\nimport hashlib\n\n\ndef _prompt_meta(text: str | None) -> dict[str, Any]:\n    \"\"\"Return ``{prompt_len, prompt_sha256}`` for a contract objective.\"\"\"\n    if text is None:\n        return {\"prompt_len\": 0, \"prompt_sha256\": \"\"}\n    data = text.encode(\"utf-8\", errors=\"replace\")\n    return {\n        \"prompt_len\": len(data),\n        \"prompt_sha256\": hashlib.sha256(data).hexdigest(),\n    }\n\n```", "chosen": "```python\nfrom solution import _prompt_meta\nimport hashlib\n\ndef test_prompt_meta_none_input():\n    \"\"\"Test that None input returns zero length and empty hash.\"\"\"\n    result = _prompt_meta(None)\n    assert result == {'prompt_len': 0, 'prompt_sha256': ''}\n\ndef test_prompt_meta_empty_string():\n    \"\"\"Test that empty string input returns zero length and correct hash.\"\"\"\n    result = _prompt_meta('')\n    assert result['prompt_len'] == 0\n    assert result['prompt_sha256'] == hashlib.sha256(b'').hexdigest()\n\ndef test_prompt_meta_non_empty_string():\n    \"\"\"Test that non-empty string returns correct length and hash.\"\"\"\n    text = 'hello world'\n    result = _prompt_meta(text)\n    assert result['prompt_len'] == len(text.encode('utf-8'))\n    assert result['prompt_sha256'] == hashlib.sha256(text.encode('utf-8')).hexdigest()\n\ndef test_prompt_meta_unicode_characters():\n    \"\"\"Test that unicode characters are handled with errors='replace'.\"\"\"\n    text = 'hello \u4e16\u754c \ud83c\udf0d'\n    result = _prompt_meta(text)\n    assert result['prompt_len'] == len(text.encode('utf-8', errors='replace'))\n    assert result['prompt_sha256'] == hashlib.sha256(text.encode('utf-8', errors='replace')).hexdigest()\n\ndef test_prompt_meta_boundary_single_char():\n    \"\"\"Test boundary case of single character input.\"\"\"\n    text = 'a'\n    result = _prompt_meta(text)\n    assert result['prompt_len'] == 1\n    assert result['prompt_sha256'] == hashlib.sha256('a'.encode('utf-8', errors='replace')).hexdigest()\n\ndef test_prompt_meta_boundary_long_string():\n    \"\"\"Test boundary case of a very long string.\"\"\"\n    text = 'x' * 10000\n    result = _prompt_meta(text)\n    assert result['prompt_len'] == 10000\n    assert result['prompt_sha256'] == hashlib.sha256(text.encode('utf-8', errors='replace')).hexdigest()\n\ndef test_prompt_meta_unicode_replace_behavior():\n    \"\"\"Test that unicode replacement is applied correctly.\"\"\"\n    text = 'hello \\x80\\x81'\n    result = _prompt_meta(text)\n    assert result['prompt_len'] == len(text.encode('utf-8', errors='replace'))\n    assert result['prompt_sha256'] == hashlib.sha256(text.encode('utf-8', errors='replace')).hexdigest()\n```", "rejected": "```python\nfrom solution import _prompt_meta\nimport pytest\n\ndef test_prompt_meta_none():\n    result = _prompt_meta(None)\n    assert result == {'prompt_len': 0, 'prompt_sha256': ''}\n\ndef test_prompt_meta_empty_string():\n    result = _prompt_meta('')\n    assert result['prompt_len'] == 0\n    assert result['prompt_sha256'] == hashlib.sha256(b'').hexdigest()\n\ndef test_prompt_meta_single_char():\n    result = _prompt_meta('a')\n    assert result['prompt_len'] == 1\n    assert result['prompt_sha256'] == hashlib.sha256(b'a').hexdigest()\n\ndef test_prompt_meta_unicode_chars():\n    result = _prompt_meta('hello \u4e16\u754c')\n    assert result['prompt_len'] == 10\n    expected_sha = hashlib.sha256('hello \u4e16\u754c'.encode('utf-8', errors='replace')).hexdigest()\n    assert result['prompt_sha256'] == expected_sha\n\ndef test_prompt_meta_special_chars():\n    result = _prompt_meta('a\\n\\t\\x7f')\n    assert result['prompt_len'] == 5\n    expected_sha = hashlib.sha256('a\\n\\t\\x7f'.encode('utf-8', errors='replace')).hexdigest()\n    assert result['prompt_sha256'] == expected_sha\n\ndef test_prompt_meta_long_string():\n    long_text = 'x' * 1000\n    result = _prompt_meta(long_text)\n    assert result['prompt_len'] == 1000\n    expected_sha = hashlib.sha256(long_text.encode('utf-8')).hexdigest()\n    assert result['prompt_sha256'] == expected_sha\n\ndef test_prompt_meta_unicode_replacement():\n    invalid = '\\uffff'\n    result = _prompt_meta(invalid)\n    assert result['prompt_len'] == 2\n    expected_sha = hashlib.sha256(invalid.encode('utf-8', errors='replace')).hexdigest()\n    assert result['prompt_sha256'] == expected_sha\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Optional\n\n\ndef _find_job(jobs: list[dict[str, Any]], job_id: str) -> Optional[dict[str, Any]]:\n    \"\"\"Find a job by id or name (case-insensitive name match).\"\"\"\n    if not job_id:\n        return None\n    needle = str(job_id).strip().lower()\n    for job in jobs:\n        if str(job.get(\"id\") or \"\") == job_id:\n            return job\n        if str(job.get(\"id\") or \"\").lower() == needle:\n            return job\n        if str(job.get(\"name\") or \"\").lower() == needle:\n            return job\n    return None\n\n```", "chosen": "```python\nfrom solution import _find_job\nimport pytest\n\ndef test_find_job_by_exact_id_match():\n    jobs = [{'id': 'job123'}]\n    assert _find_job(jobs, 'job123') == {'id': 'job123'}\n\ndef test_find_job_by_id_with_different_case():\n    jobs = [{'id': 'Job123'}]\n    assert _find_job(jobs, 'job123') == {'id': 'Job123'}\n\ndef test_find_job_by_id_with_extra_spaces():\n    jobs = [{'id': '  job456  '}]\n    assert _find_job(jobs, '  job456  ') == {'id': '  job456  '}\n\ndef test_find_job_by_id_when_no_match():\n    jobs = [{'id': 'job123'}]\n    assert _find_job(jobs, 'nonexistent') is None\n\ndef test_find_job_by_name_match_case_insensitive():\n    jobs = [{'name': 'My Job', 'id': 'job123'}]\n    assert _find_job(jobs, 'my job') == {'name': 'My Job', 'id': 'job123'}\n\ndef test_find_job_by_id_when_id_is_none_in_dict():\n    jobs = [{'name': 'Job A'}]\n    assert _find_job(jobs, 'Job A') == {'name': 'Job A'}\n\ndef test_find_job_when_jobs_list_empty():\n    assert _find_job([], 'any-id') is None\n\ndef test_find_job_when_id_is_empty_string():\n    jobs = [{'id': 'job123'}]\n    assert _find_job(jobs, '') is None\n```", "rejected": "```python\nimport pytest\n\ndef test_find_job_matches_id_exact():\n    jobs = [{'id': 'job1', 'name': 'job one'}]\n    assert _find_job(jobs, 'job1') is not None\n    assert _find_job(jobs, 'job1')['id'] == 'job1'\n\ndef test_find_job_matches_id_case_insensitive():\n    jobs = [{'id': 'Job1', 'name': 'job one'}]\n    assert _find_job(jobs, 'JOB1') is not None\n    assert _find_job(jobs, 'job1') is not None\n\ndef test_find_job_matches_name_case_insensitive():\n    jobs = [{'id': 'job1', 'name': 'Job One'}]\n    assert _find_job(jobs, 'job one') is not None\n    assert _find_job(jobs, 'JOB ONE') is not None\n\ndef test_find_job_returns_none_for_empty_id():\n    jobs = [{'id': 'job1', 'name': 'job one'}]\n    assert _find_job(jobs, '') is None\n\ndef test_find_job_returns_none_for_nonexistent():\n    jobs = [{'id': 'job1', 'name': 'job one'}]\n    assert _find_job(jobs, 'nonexistent') is None\n\ndef test_find_job_none_when_not_found():\n    jobs = [{'id': 'job1', 'name': 'job one'}]\n    assert _find_job(jobs, 'job2') is None\n\ndef test_find_job_with_empty_name_in_job():\n    jobs = [{'id': 'job1', 'name': ''}]\n    assert _find_job(jobs, '') is None\n\ndef test_find_job_with_none_id_in_job():\n    jobs = [{'id': None, 'name': 'job one'}]\n    assert _find_job(jobs, 'job1') is None\n    assert _find_job(jobs, 'Job1') is None\n    assert _find_job(jobs, '') is None\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Optional\n\n\ndef _find_job(jobs: list[dict[str, Any]], job_id: str) -> Optional[dict[str, Any]]:\n    \"\"\"Find a job by id or name (case-insensitive name match).\"\"\"\n    if not job_id:\n        return None\n    needle = str(job_id).strip().lower()\n    for job in jobs:\n        if str(job.get(\"id\") or \"\") == job_id:\n            return job\n        if str(job.get(\"id\") or \"\").lower() == needle:\n            return job\n        if str(job.get(\"name\") or \"\").lower() == needle:\n            return job\n    return None\n\n```", "chosen": "```python\nimport pytest\nfrom solution import _find_job\n\ndef test_find_job_by_exact_id():\n    jobs = [{'id': 'job-1', 'name': 'Job A'}]\n    result = _find_job(jobs, 'job-1')\n    assert result is not None\n    assert result['id'] == 'job-1'\n    assert result['name'] == 'Job A'\n\ndef test_find_job_by_case_insensitive_id():\n    jobs = [{'id': 'Job-1', 'name': 'Job A'}]\n    result = _find_job(jobs, 'job-1')\n    assert result is not None\n    assert result['id'] == 'Job-1'\n\ndef test_find_job_by_case_insensitive_id_lower():\n    jobs = [{'id': 'JOB-1', 'name': 'Job A'}]\n    result = _find_job(jobs, 'job-1')\n    assert result is not None\n    assert result['id'] == 'JOB-1'\n\ndef test_find_job_by_name_exact():\n    jobs = [{'id': 'job-1', 'name': 'Job B'}]\n    result = _find_job(jobs, 'Job B')\n    assert result is not None\n    assert result['name'] == 'Job B'\n\ndef test_find_job_by_name_case_insensitive():\n    jobs = [{'id': 'job-1', 'name': 'JOB-B'}]\n    result = _find_job(jobs, 'job-b')\n    assert result is not None\n    assert result['name'] == 'JOB-B'\n\ndef test_find_job_with_empty_id():\n    jobs = [{'id': 'job-1', 'name': 'Job A'}]\n    result = _find_job(jobs, '')\n    assert result is None\n\ndef test_find_job_with_whitespace_id():\n    jobs = [{'id': '  job-1  ', 'name': 'Job A'}]\n    result = _find_job(jobs, '  job-1  ')\n    assert result is not None\n    assert result['id'] == '  job-1  '\n\ndef test_find_job_not_found():\n    jobs = [{'id': 'job-1', 'name': 'Job A'}]\n    result = _find_job(jobs, 'nonexistent')\n    assert result is None\n```", "rejected": "```python\nimport pytest\nfrom solution import _find_job\n\ndef test_find_job_by_exact_id():\n    jobs = [{'id': 'job1', 'name': 'Alpha'}]\n    result = _find_job(jobs, 'job1')\n    assert result is not None\n    assert result['id'] == 'job1'\n\ndef test_find_job_by_id_with_spaces():\n    jobs = [{'id': 'job 1', 'name': 'Alpha'}]\n    result = _find_job(jobs, '  job 1  ')\n    assert result is not None\n    assert result['id'] == 'job 1'\n\ndef test_find_job_by_case_insensitive_id():\n    jobs = [{'id': 'JOB1', 'name': 'Alpha'}]\n    result = _find_job(jobs, 'job1')\n    assert result is not None\n    assert result['id'] == 'JOB1'\n\ndef test_find_job_by_case_insensitive_id_lower():\n    jobs = [{'id': 'Job1', 'name': 'Alpha'}]\n    result = _find_job(jobs, 'job1')\n    assert result is not None\n    assert result['id'] == 'Job1'\n\ndef test_find_job_by_name_case_insensitive():\n    jobs = [{'id': '123', 'name': 'Beta'}]\n    result = _find_job(jobs, 'beta')\n    assert result is not None\n    assert result['name'] == 'Beta'\n\ndef test_find_job_by_name_with_spaces_and_case():\n    jobs = [{'id': '1', 'name': '  Gamma  '}]\n    result = _find_job(jobs, 'GAMMA')\n    assert result is not None\n    assert result['name'] == '  Gamma  '\n\ndef test_find_job_not_found():\n    jobs = [{'id': 'job1', 'name': 'Alpha'}]\n    result = _find_job(jobs, 'job999')\n    assert result is None\n\ndef test_find_job_empty_id():\n    jobs = [{'id': 'job1', 'name': 'Alpha'}]\n    result = _find_job(jobs, '')\n    assert result is None\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\ndef _hermes_argv(profile: str, sub: list[str]) -> list[str]:\n    \"\"\"Build a fixed-argv Hermes CLI invocation for the given profile.\"\"\"\n    if profile == \"default\":\n        return [\"hermes\", *sub]\n    return [\"hermes\", \"-p\", profile, *sub]\n\n```", "chosen": "```python\nfrom solution import _hermes_argv\n\ndef test_default_profile_no_sub():\n    assert _hermes_argv('default', []) == ['hermes']\n\ndef test_default_profile_with_sub():\n    assert _hermes_argv('default', ['--arg', 'value']) == ['hermes', '--arg', 'value']\n\ndef test_non_default_profile_no_sub():\n    assert _hermes_argv('custom', []) == ['hermes', '-p', 'custom']\n\ndef test_non_default_profile_with_sub():\n    assert _hermes_argv('prod', ['--mode', 'prod']) == ['hermes', '-p', 'prod', '--mode', 'prod']\n\ndef test_empty_sub_with_non_default():\n    assert _hermes_argv('staging', []) == ['hermes', '-p', 'staging']\n\ndef test_multiple_args_in_sub():\n    assert _hermes_argv('dev', ['-v', '-d', 'data']) == ['hermes', '-p', 'dev', '-v', '-d', 'data']\n\ndef test_special_chars_in_profile():\n    assert _hermes_argv('prod!@#', []) == ['hermes', '-p', 'prod!@#']\n```", "rejected": "```python\ndef test_hermes_default_profile():\n    assert _hermes_argv('default', []) == ['hermes']\n    assert _hermes_argv('default', ['arg1', 'arg2']) == ['hermes', 'arg1', 'arg2']\n\ndef test_hermes_custom_profile():\n    assert _hermes_argv('custom', []) == ['hermes', '-p', 'custom']\n    assert _hermes_argv('custom', ['arg1']) == ['hermes', '-p', 'custom', 'arg1']\n    assert _hermes_argv('custom', []) == ['hermes', '-p', 'custom']\n\ndef test_hermes_empty_sublist():\n    assert _hermes_argv('default', []) == ['hermes']\n    assert _hermes_argv('profile1', []) == ['hermes', '-p', 'profile1']\n\ndef test_hermes_non_default_profile():\n    assert _hermes_argv('test', []) == ['hermes', '-p', 'test']\n    assert _hermes_argv('prod', ['x']) == ['hermes', '-p', 'prod', 'x']\n\ndef test_hermes_empty_profile_with_sub():\n    assert _hermes_argv('default', ['a', 'b', 'c']) == ['hermes', 'a', 'b', 'c']\n\ndef test_hermes_single_arg_in_sub():\n    assert _hermes_argv('default', ['single']) == ['hermes', 'single']\n    assert _hermes_argv('profile', ['single']) == ['hermes', '-p', 'profile', 'single']\n\ndef test_hermes_non_default_with_no_sub():\n    assert _hermes_argv('myprofile', []) == ['hermes', '-p', 'myprofile']\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Optional\n\n\ndef _is_duplicate(\n    target_jobs: list[dict[str, Any]], source_job: dict[str, Any]\n) -> bool:\n    \"\"\"Return True if target has an active job with same name + schedule.\"\"\"\n    src_name = str(source_job.get(\"name\") or \"\").lower()\n    src_sched = str(source_job.get(\"schedule_display\") or source_job.get(\"schedule\") or \"\").lower()\n    for job in target_jobs:\n        if not job.get(\"enabled\", True):\n            continue\n        tgt_name = str(job.get(\"name\") or \"\").lower()\n        tgt_sched = str(job.get(\"schedule_display\") or job.get(\"schedule\") or \"\").lower()\n        if tgt_name == src_name and tgt_sched == src_sched:\n            return True\n    return False\n\n```", "chosen": "```python\nimport pytest\nfrom solution import _is_duplicate\n\ndef test_no_duplicate_with_different_names():\n    target_jobs = [{'name': 'JobA', 'enabled': True}]\n    source_job = {'name': 'JobB', 'schedule_display': 'Daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_no_duplicate_with_different_schedules():\n    target_jobs = [{'name': 'JobA', 'schedule': 'Weekly'}]\n    source_job = {'name': 'JobA', 'schedule_display': 'Daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_duplicate_with_matching_name_and_schedule():\n    target_jobs = [{'name': 'JobA', 'schedule': 'Daily'}]\n    source_job = {'name': 'JobA', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_duplicate_with_case_insensitive_matching():\n    target_jobs = [{'name': 'JobA', 'schedule_display': 'Daily'}]\n    source_job = {'name': 'joba', 'schedule': 'DAILY'}\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_non_enabled_target_job_is_ignored():\n    target_jobs = [{'name': 'JobA', 'enabled': False}]\n    source_job = {'name': 'JobA', 'schedule': 'Daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_empty_target_jobs_list():\n    target_jobs = []\n    source_job = {'name': 'JobA', 'schedule_display': 'Daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_target_jobs_all_disabled():\n    target_jobs = [{'name': 'JobA', 'enabled': False}, {'name': 'JobB', 'enabled': False}]\n    source_job = {'name': 'JobA', 'schedule': 'Daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_partial_key_missing_in_source():\n    target_jobs = [{'name': 'JobA', 'schedule': 'Daily', 'enabled': True}]\n    source_job = {'name': 'JobA', 'schedule_display': 'Daily'}\n    assert _is_duplicate(target_jobs, source_job) is True\n```", "rejected": "```python\nimport pytest\nfrom solution import _is_duplicate\n\ndef test_duplicate_with_enabled_job():\n    \"\"\"Test that a duplicate is found when the target job is enabled.\"\"\"\n    target_jobs = [{'name': 'Job A', 'enabled': True}]\n    source_job = {'name': 'job a', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_duplicate_with_disabled_job():\n    \"\"\"Test that a disabled target job is skipped.\"\"\"\n    target_jobs = [{'name': 'Job A', 'enabled': False}]\n    source_job = {'name': 'job a', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_duplicate_with_schedule_display_mismatch():\n    \"\"\"Test that different schedules prevent a match.\"\"\"\n    target_jobs = [{'name': 'Job A', 'enabled': True, 'schedule_display': 'weekly'}]\n    source_job = {'name': 'job a', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_duplicate_with_schedule_mismatch():\n    \"\"\"Test that different schedules prevent a match.\"\"\"\n    target_jobs = [{'name': 'Job A', 'enabled': True, 'schedule': 'weekly'}]\n    source_job = {'name': 'job a', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_duplicate_with_name_case_insensitive():\n    \"\"\"Test that name comparison is case-insensitive.\"\"\"\n    target_jobs = [{'name': 'JOB A', 'enabled': True}]\n    source_job = {'name': 'job a', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_duplicate_with_schedule_case_insensitive():\n    \"\"\"Test that schedule comparison is case-insensitive.\"\"\"\n    target_jobs = [{'name': 'Job A', 'enabled': True, 'schedule_display': 'DAILY'}]\n    source_job = {'name': 'JOB A', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_empty_target_jobs():\n    \"\"\"Test that an empty list of target jobs returns False.\"\"\"\n    target_jobs = []\n    source_job = {'name': 'job a', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_no_duplicate_found():\n    \"\"\"Test that no duplicate is found when jobs differ.\"\"\"\n    target_jobs = [{'name': 'Job B', 'enabled': True, 'schedule_display': 'hourly'}]\n    source_job = {'name': 'job a', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Optional\n\n\ndef _is_duplicate(\n    target_jobs: list[dict[str, Any]], source_job: dict[str, Any]\n) -> bool:\n    \"\"\"Return True if target has an active job with same name + schedule.\"\"\"\n    src_name = str(source_job.get(\"name\") or \"\").lower()\n    src_sched = str(source_job.get(\"schedule_display\") or source_job.get(\"schedule\") or \"\").lower()\n    for job in target_jobs:\n        if not job.get(\"enabled\", True):\n            continue\n        tgt_name = str(job.get(\"name\") or \"\").lower()\n        tgt_sched = str(job.get(\"schedule_display\") or job.get(\"schedule\") or \"\").lower()\n        if tgt_name == src_name and tgt_sched == src_sched:\n            return True\n    return False\n\n```", "chosen": "```python\nimport pytest\nfrom solution import _is_duplicate\n\ndef test_basic_match_enabled():\n    \"\"\"Test case where a duplicate active job exists.\"\"\"\n    target_jobs = [{'name': 'MyJob', 'schedule': 'daily', 'enabled': True}]\n    source_job = {'name': 'MYJOB', 'schedule_display': 'DAILY'}\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_basic_match_disabled():\n    \"\"\"Test case where a duplicate job is disabled.\"\"\"\n    target_jobs = [{'name': 'MyJob', 'schedule': 'daily', 'enabled': False}]\n    source_job = {'name': 'MYJOB', 'schedule_display': 'DAILY'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_case_insensitive_names():\n    \"\"\"Test case insensitivity for job names.\"\"\"\n    target_jobs = [{'name': 'MyJob', 'schedule': 'daily', 'enabled': True}]\n    source_job = {'name': 'myjob', 'schedule_display': 'DAILY'}\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_case_insensitive_schedules():\n    \"\"\"Test case insensitivity for schedule values.\"\"\"\n    target_jobs = [{'name': 'MyJob', 'schedule': 'daily', 'enabled': True}]\n    source_job = {'name': 'MyJob', 'schedule_display': 'Daily'}\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_empty_source_job():\n    \"\"\"Test when source_job has no name or schedule.\"\"\"\n    target_jobs = [{'name': 'OtherJob', 'schedule': 'weekly', 'enabled': True}]\n    source_job = {}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_different_schedules():\n    \"\"\"Test when schedules differ.\"\"\"\n    target_jobs = [{'name': 'MyJob', 'schedule': 'daily', 'enabled': True}]\n    source_job = {'name': 'MyJob', 'schedule_display': 'weekly'}\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_nested_schedule_display():\n    \"\"\"Test when schedule_display takes precedence over schedule.\"\"\"\n    target_jobs = [{'name': 'MyJob', 'schedule_display': 'custom', 'enabled': True}]\n    source_job = {'name': 'MyJob', 'schedule_display': 'custom'}\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_no_duplicate_in_target():\n    \"\"\"Test when no matching job exists in target.\"\"\"\n    target_jobs = [{'name': 'OtherJob', 'schedule': 'weekly', 'enabled': True}]\n    source_job = {'name': 'MyJob', 'schedule_display': 'daily'}\n    assert _is_duplicate(target_jobs, source_job) is False\n```", "rejected": "```python\nimport pytest\nfrom solution import _is_duplicate\n\ndef test_duplicate_true():\n    \"\"\"Test case where a duplicate active job exists.\"\"\"\n    source_job = {'name': 'MyJob', 'schedule': 'daily'}\n    target_jobs = [{'name': 'MyJob', 'schedule': 'daily', 'enabled': True}]\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_duplicate_true_case_insensitive():\n    \"\"\"Test case insensitivity for name and schedule.\"\"\"\n    source_job = {'name': 'MYJOB', 'schedule': 'DAILY'}\n    target_jobs = [{'name': 'myjob', 'schedule': 'daily', 'enabled': True}]\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_no_duplicate():\n    \"\"\"Test case where no duplicate exists.\"\"\"\n    source_job = {'name': 'MyJob', 'schedule': 'daily'}\n    target_jobs = [{'name': 'OtherJob', 'schedule': 'weekly', 'enabled': True}]\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_disabled_job_skipped():\n    \"\"\"Test that disabled jobs are skipped.\"\"\"\n    source_job = {'name': 'MyJob', 'schedule': 'daily'}\n    target_jobs = [{'name': 'MyJob', 'schedule': 'daily', 'enabled': False}]\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_empty_target_jobs():\n    \"\"\"Test with empty target jobs list.\"\"\"\n    source_job = {'name': 'MyJob', 'schedule': 'daily'}\n    target_jobs = []\n    assert _is_duplicate(target_jobs, source_job) is False\n\ndef test_source_job_with_missing_name():\n    \"\"\"Test source job with missing name field.\"\"\"\n    source_job = {'schedule': 'daily'}\n    target_jobs = [{'name': 'MyJob', 'schedule': 'daily', 'enabled': True}]\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_source_job_with_missing_schedule():\n    \"\"\"Test source job with missing schedule field.\"\"\"\n    source_job = {'name': 'MyJob'}\n    target_jobs = [{'name': 'MyJob', 'schedule': 'daily', 'enabled': True}]\n    assert _is_duplicate(target_jobs, source_job) is True\n\ndef test_mixed_case_schedule_hierarchy():\n    \"\"\"Test schedule_display takes precedence over schedule.\"\"\"\n    source_job = {'name': 'MyJob', 'schedule_display': 'CustomSchedule', 'schedule': 'default'}\n    target_jobs = [{'name': 'MyJob', 'schedule_display': 'CustomSchedule', 'schedule': 'default', 'enabled': True}]\n    assert _is_duplicate(target_jobs, source_job) is True\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nimport re\n\n\ndef redact_output(text: str) -> str:\n    \"\"\"Best-effort redaction of secret-looking substrings in command output.\"\"\"\n    if not text:\n        return \"\"\n    # Redact common secret shapes: long hex/base64 strings after key/token-like\n    # labels, Bearer tokens, sk-... / sk-proj-... OpenAI keys, AKIA... AWS keys.\n    patterns: list[tuple[str, str]] = [\n        (r\"(?i)\\b(sk(?:-proj)?-[A-Za-z0-9_-]{20,})\\b\", \"[REDACTED_OPENAI_KEY]\"),\n        (r\"(?i)\\b(AKIA[0-9A-Z.]{6,})\\b\", \"[REDACTED_AWS_KEY]\"),\n        (r\"(?i)\\b(AKIA[0-9A-Z]{16})\\b\", \"[REDACTED_AWS_KEY]\"),\n        (r\"(?i)(\\bBearer\\s+)([A-Za-z0-9._\\-]{16,})\\b\", r\"\\1[REDACTED]\"),\n        (r\"(?i)(\\b(?:token|secret|password|api[_-]?key|passwd)\\s*[:=]\\s*[\\\"']?)([^\\s\\\"']{8,})\", r\"\\1[REDACTED]\"),\n    ]\n    out = text\n    for pattern, repl in patterns:\n        out = re.sub(pattern, repl, out)\n    return out\n\n```", "chosen": "```python\nimport pytest\nfrom solution import redact_output\n\ndef test_empty_string():\n    assert redact_output('') == ''\n\ndef test_no_secrets():\n    assert redact_output('This is just normal text with no secrets.') == 'This is just normal text with no secrets.'\n\ndef test_openai_key_short():\n    assert redact_output('sk-test1234567890123') == 'sk-test1234567890123'\n\ndef test_openai_key_exact_length():\n    assert redact_output('sk-abcde123456789012345') == '[REDACTED_OPENAI_KEY]'\n\ndef test_openai_key_longer():\n    assert redact_output('sk-abcde123456789012345678') == '[REDACTED_OPENAI_KEY]'\n\ndef test_aws_key_v2():\n    assert redact_output('AKIAI44QH8D2EXAMPLE') == '[REDACTED_AWS_KEY]'\n\ndef test_aws_key_long():\n    assert redact_output('AKIAI44QH8D2EXAMPLE12345') == '[REDACTED_AWS_KEY]'\n\ndef test_bearer_token():\n    assert redact_output('Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyfQ.SflKxwRJeD390040WAdBxj3U95901') == 'Bearer [REDACTED]'\n```", "rejected": "```python\nfrom solution import redact_output\n\ndef test_redact_openai_key():\n    text = 'key=sk-proj-abcdefghijklmnopqrstuvwx'\n    assert redact_output(text) == 'key=[REDACTED_OPENAI_KEY]'\n\ndef test_redact_aws_key_v1():\n    text = 'access_key=AKIA12345678901234'\n    assert redact_output(text) == 'access_key=[REDACTED_AWS_KEY]'\n\ndef test_redact_aws_key_v2():\n    text = 'aws_key=AKIAABCDEFGHIJ1234567890'\n    assert redact_output(text) == 'aws_key=[REDACTED_AWS_KEY]'\n\ndef test_redact_bearer_token():\n    text = 'Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyfQ.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c'\n    assert redact_output(text) == 'Authorization: Bearer [REDACTED]'\n\ndef test_redact_secret_placeholder():\n    text = 'SECRET_KEY=abcdef123456789012345678'\n    assert redact_output(text) == 'SECRET_KEY=[REDACTED]'\n\ndef test_no_redaction_safe_text():\n    text = 'This is a normal command output without any secrets.'\n    assert redact_output(text) == 'This is a normal command output without any secrets.'\n\ndef test_empty_and_whitespace():\n    assert redact_output('') == ''\n    assert redact_output('   ') == '   '\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nimport re\n\n\ndef strip_nonsemantic_markdown(text: str) -> str:\n    \"\"\"Remove HTML comments and fenced code before checking semantic markers.\"\"\"\n\n    text = re.sub(r\"<!--.*?-->\", \"\", text, flags=re.DOTALL)\n    output: list[str] = []\n    fence_char: str | None = None\n    fence_length = 0\n\n    for line in text.splitlines(keepends=True):\n        match = re.match(r\"^\\s{0,3}(`{3,}|~{3,})(.*)$\", line.rstrip(\"\\r\\n\"))\n        if fence_char is None and match:\n            marker = match.group(1)\n            fence_char = marker[0]\n            fence_length = len(marker)\n            output.append(\"\\n\" if line.endswith(\"\\n\") else \"\")\n            continue\n        if fence_char is not None:\n            closing = re.match(\n                r\"^\\s{0,3}([`~]{3,})\\s*$\", line.rstrip(\"\\r\\n\")\n            )\n            if (\n                closing\n                and closing.group(1)[0] == fence_char\n                and len(closing.group(1)) >= fence_length\n            ):\n                fence_char = None\n                fence_length = 0\n            output.append(\"\\n\" if line.endswith(\"\\n\") else \"\")\n        else:\n            output.append(line)\n\n    return \"\".join(output)\n\n```", "chosen": "```python\nfrom solution import strip_nonsemantic_markdown\n\ndef test_no_fenced_code():\n    assert strip_nonsemantic_markdown('Hello\\nWorld') == 'Hello\\nWorld'\n\ndef test_html_comments():\n    assert strip_nonsemantic_markdown('Hello <!-- comment --> World') == 'Hello  World'\n```", "rejected": "```python\nfrom solution import strip_nonsemantic_markdown\n\ndef test_strip_html_comments():\n    text = '<!-- comment -->\\n\\nSome text'\n    assert strip_nonsemantic_markdown(text) == '\\n\\nSome text'\n\ndef test_strip_html_comments_with_multiline():\n    text = '<!-- comment 1 -->\\n\\n<!-- comment 2 -->\\n\\nSome text'\n    assert strip_nonsemantic_markdown(text) == '\\n\\n\\nSome text'\n\ndef test_no_html_comments():\n    text = 'Normal text'\n    assert strip_nonsemantic_markdown(text) == 'Normal text'\n\ndef test_no_fenced_code():\n    text = 'No fenced code\\n\\nJust text'\n    assert strip_nonsemantic_markdown(text) == 'No fenced code\\n\\nJust text'\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nimport re\n\n\ndef strip_nonsemantic_markdown(text: str) -> str:\n    \"\"\"Remove HTML comments and fenced code before checking semantic markers.\"\"\"\n\n    text = re.sub(r\"<!--.*?-->\", \"\", text, flags=re.DOTALL)\n    output: list[str] = []\n    fence_char: str | None = None\n    fence_length = 0\n\n    for line in text.splitlines(keepends=True):\n        match = re.match(r\"^\\s{0,3}(`{3,}|~{3,})(.*)$\", line.rstrip(\"\\r\\n\"))\n        if fence_char is None and match:\n            marker = match.group(1)\n            fence_char = marker[0]\n            fence_length = len(marker)\n            output.append(\"\\n\" if line.endswith(\"\\n\") else \"\")\n            continue\n        if fence_char is not None:\n            closing = re.match(\n                r\"^\\s{0,3}([`~]{3,})\\s*$\", line.rstrip(\"\\r\\n\")\n            )\n            if (\n                closing\n                and closing.group(1)[0] == fence_char\n                and len(closing.group(1)) >= fence_length\n            ):\n                fence_char = None\n                fence_length = 0\n            output.append(\"\\n\" if line.endswith(\"\\n\") else \"\")\n        else:\n            output.append(line)\n\n    return \"\".join(output)\n\n```", "chosen": "```python\nimport pytest\nfrom solution import strip_nonsemantic_markdown\n\ndef test_basic_markdown_preserved():\n    text = '# Header\\nSome text\\n[Link](url)'\n    assert strip_nonsemantic_markdown(text) == text\n\ndef test_html_comments_removed():\n    text = '<!-- comment --># Header\\n<!-- another -->Some text'\n    assert strip_nonsemantic_markdown(text) == '# Header\\nSome text'\n```", "rejected": "```python\nfrom solution import strip_nonsemantic_markdown\n\ndef test_basic_text_without_markdown():\n    assert strip_nonsemantic_markdown('Hello\\nWorld') == 'Hello\\nWorld'\n\ndef test_html_comments_removed():\n    assert strip_nonsemantic_markdown('<!-- comment -->\\nHello\\n<!-- another -->\\nWorld') == 'Hello\\nWorld'\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Mapping, Sequence\n\n\ndef _a22_stat_labels(value: Any) -> Any:\n    \"\"\"Relabel reused numeric bootstrap machinery without A9 evidence semantics.\"\"\"\n\n    if isinstance(value, dict):\n        renamed = {\"a9_mean\": \"a22_mean\", \"a9_ratio\": \"a22_ratio\"}\n        return {\n            renamed.get(str(key), str(key)): _a22_stat_labels(item)\n            for key, item in value.items()\n        }\n    if isinstance(value, list):\n        return [_a22_stat_labels(item) for item in value]\n    if isinstance(value, str):\n        if value == \"A9_minus_A8\":\n            return \"A22_minus_A8\"\n        if value.startswith(\"multitown-a9-\"):\n            return \"multitown-a22-\" + value.removeprefix(\"multitown-a9-\")\n    return value\n\n```", "chosen": "```python\nfrom solution import _a22_stat_labels\n\ndef test_dict_renaming():\n    d = {'a9_mean': 1.5, 'other': {}}\n    result = _a22_stat_labels(d)\n    assert result == {'a22_mean': 1.5, 'other': {}}\n\ndef test_nested_dict_renaming():\n    d = {'a9_mean': {'a9_ratio': 0.5, 'other': 10}}\n    result = _a22_stat_labels(d)\n    assert result == {'a22_mean': {'a22_ratio': 0.5, 'other': 10}}\n\ndef test_list_relabeling():\n    lst = [1, 'A9_minus_A8', 'multitown-a9-test']\n    result = _a22_stat_labels(lst)\n    assert result == [1, 'A22_minus_A8', 'multitown-a22-test']\n\ndef test_string_partial_relabeling():\n    result = _a22_stat_labels('A9_minus_A8')\n    assert result == 'A22_minus_A8'\n\ndef test_string_prefix_relabeling():\n    result = _a22_stat_labels('multitown-a9-foo')\n    assert result == 'multitown-a22-foo'\n\ndef test_non_relabelable_values():\n    result = _a22_stat_labels(42)\n    assert result == 42\n\ndef test_empty_inputs():\n    assert _a22_stat_labels({}) == {}\n    assert _a22_stat_labels([]) == []\n\ndef test_string_literal_values():\n    result = _a22_stat_labels('random')\n    assert result == 'random'\n```", "rejected": "```python\nimport pytest\nfrom solution import _a22_stat_labels\n\ndef test_dict_basic_rename():\n    data = {'a9_mean': 1.0, 'a9_ratio': 2.0, 'other': 3.0}\n    result = _a22_stat_labels(data)\n    assert result == {'a22_mean': 1.0, 'a22_ratio': 2.0, 'other': 3.0}\n\ndef test_dict_nested_relabel():\n    data = {'a9_mean': {'a9_ratio': 1.0, 'other': 2.0}}\n    result = _a22_stat_labels(data)\n    assert result == {'a22_mean': {'a22_ratio': 1.0, 'other': 2.0}}\n\ndef test_list_basic_relabel():\n    data = [1, 'A9_minus_A8', 3.0]\n    result = _a22_stat_labels(data)\n    assert result == [1, 'A22_minus_A8', 3.0]\n\ndef test_string_relabel_exact():\n    result = _a22_stat_labels('A9_minus_A8')\n    assert result == 'A22_minus_A8'\n\ndef test_string_relabel_prefix():\n    result = _a22_stat_labels('multitown-a9-test')\n    assert result == 'multitown-a22-test'\n\ndef test_string_no_relabel():\n    result = _a22_stat_labels('other_string')\n    assert result == 'other_string'\n\ndef test_non_relabelable_types():\n    result = _a22_stat_labels(42)\n    assert result == 42\n    result = _a22_stat_labels(None)\n    assert result is None\n    result = _a22_stat_labels(obj=object())\n    assert result is obj\n\ndef test_empty_collections():\n    result = _a22_stat_labels({})\n    assert result == {}\n    result = _a22_stat_labels([])\n    assert result == []\n    result = _a22_stat_labels('')\n    assert result == ''\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Mapping, Sequence\n\n\ndef _a22_stat_labels(value: Any) -> Any:\n    \"\"\"Relabel reused numeric bootstrap machinery without A9 evidence semantics.\"\"\"\n\n    if isinstance(value, dict):\n        renamed = {\"a9_mean\": \"a22_mean\", \"a9_ratio\": \"a22_ratio\"}\n        return {\n            renamed.get(str(key), str(key)): _a22_stat_labels(item)\n            for key, item in value.items()\n        }\n    if isinstance(value, list):\n        return [_a22_stat_labels(item) for item in value]\n    if isinstance(value, str):\n        if value == \"A9_minus_A8\":\n            return \"A22_minus_A8\"\n        if value.startswith(\"multitown-a9-\"):\n            return \"multitown-a22-\" + value.removeprefix(\"multitown-a9-\")\n    return value\n\n```", "chosen": "```python\nfrom solution import _a22_stat_labels\n\ndef test_dict_basic_relabel():\n    data = {'a9_mean': 10, 'a9_ratio': 0.5, 'other': 'value'}\n    result = _a22_stat_labels(data)\n    assert result == {'a22_mean': 10, 'a22_ratio': 0.5, 'other': 'value'}\n\ndef test_dict_nested_relabel():\n    data = {'a9_mean': {'a9_ratio': 0.3, 'other': 2}, 'other_key': 1}\n    result = _a22_stat_labels(data)\n    assert result == {'a22_mean': {'a22_ratio': 0.3, 'other': 2}, 'other_key': 1}\n\ndef test_list_relabel():\n    data = ['A9_minus_A8', 'multitown-a9-test', 'other', 1, 2.0]\n    result = _a22_stat_labels(data)\n    assert result == ['A22_minus_A8', 'multitown-a22-test', 'other', 1, 2.0]\n\ndef test_string_exact_match():\n    result = _a22_stat_labels('A9_minus_A8')\n    assert result == 'A22_minus_A8'\n\ndef test_string_prefix_relabel():\n    result = _a22_stat_labels('multitown-a9-something')\n    assert result == 'multitown-a22-something'\n\ndef test_string_other_unchanged():\n    result = _a22_stat_labels('other_string')\n    assert result == 'other_string'\n\ndef test_non_map_types_unchanged():\n    result = ['int', 'float', 'bool', None, {'key': 'val'}, {'a9_mean': 1}]\n    expected = ['int', 'float', 'bool', None, {'key': 'val'}, {'a22_mean': 1}]\n    assert _a22_stat_labels(result) == expected\n\ndef test_empty_structures():\n    data = {'a9_mean': [], 'a9_ratio': [], 'other': []}\n    result = _a22_stat_labels(data)\n    assert result == {'a22_mean': [], 'a22_ratio': [], 'other': []}\n```", "rejected": "```python\nfrom solution import _a22_stat_labels\n\ndef test_dict_renames_keys():\n    data = {'a9_mean': 10, 'a9_ratio': 0.5, 'other': 'value'}\n    result = _a22_stat_labels(data)\n    assert result == {'a22_mean': 10, 'a22_ratio': 0.5, 'other': 'value'}\n\ndef test_dict_nested_relabels():\n    data = {'a9_mean': 10, 'a9_ratio': 0.5, 'sub': {'a9_mean': 20}}\n    result = _a22_stat_labels(data)\n    assert result == {'a22_mean': 10, 'a22_ratio': 0.5, 'sub': {'a22_mean': 20}}\n\ndef test_list_relabels_items():\n    data = [1, 2, 'A9_minus_A8', 'multitown-a9-test']\n    result = _a22_stat_labels(data)\n    assert result == [1, 2, 'A22_minus_A8', 'multitown-a22-test']\n\ndef test_string_exact_match():\n    assert _a22_stat_labels('A9_minus_A8') == 'A22_minus_A8'\n\ndef test_string_prefix_replace():\n    assert _a22_stat_labels('multitown-a9-something') == 'multitown-a22-something'\n\ndef test_string_unchanged():\n    assert _a22_stat_labels('other-string') == 'other-string'\n    assert _a22_stat_labels('') == ''\n    assert _a22_stat_labels('A8') == 'A8'\n\ndef test_nested_mixed_structure():\n    data = {'a9_mean': [1, 2, 3], 'a9_ratio': {'x': 'A9_minus_A8', 'y': 'multitown-a9-z'}, 'leaf': 42}\n    result = _a22_stat_labels(data)\n    assert result == {'a22_mean': [1, 2, 3], 'a22_ratio': {'x': 'A22_minus_A8', 'y': 'multitown-a22-z'}, 'leaf': 42}\n\ndef test_other_types_unchanged():\n    assert _a22_stat_labels(int) == int\n    assert _a22_stat_labels(float) == 3.14\n    assert _a22_stat_labels(None) is None\n    assert _a22_stat_labels(1.222) == 1.222\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nfrom typing import Any, Callable, Iterable, Mapping, Sequence\nimport copy\n\n\ndef alpha_normalize_idempotency_keys(\n    actions: Sequence[Mapping[str, Any]],\n) -> tuple[dict[str, Any], ...]:\n    \"\"\"Canonicalize key equality patterns without claiming Unicode completeness.\"\"\"\n\n    names: dict[str, str] = {}\n    rows: list[dict[str, Any]] = []\n    for action in actions:\n        row = copy.deepcopy(dict(action))\n        key = row.get(\"idempotency_key\")\n        if isinstance(key, str):\n            names.setdefault(key, f\"K{len(names)}\")\n            row[\"idempotency_key\"] = names[key]\n        rows.append(row)\n    return tuple(rows)\n\n```", "chosen": "```python\nfrom solution import alpha_normalize_idempotency_keys\nimport pytest\n\ndef test_basic_case():\n    actions = [{'idempotency_key': 'A', 'payload': 1}, {'idempotency_key': 'B', 'payload': 2}, {'idempotency_key': 'A', 'payload': 3}]\n    result = alpha_normalize_idempotency_keys(actions)\n    assert result == ({'idempotency_key': 'K0', 'payload': 1}, {'idempotency_key': 'K1', 'payload': 2}, {'idempotency_key': 'K0', 'payload': 3})\n\ndef test_empty_sequence():\n    result = alpha_normalize_idempotency_keys([])\n    assert result == ()\n\ndef test_no_idempotency_key():\n    actions = [{'foo': 'bar'}, {'baz': 'qux'}]\n    result = alpha_normalize_idempotency_keys(actions)\n    assert result == ({'foo': 'bar'}, {'baz': 'qux'})\n\ndef test_non_string_key():\n    actions = [{'idempotency_key': 123, 'payload': 1}, {'idempotency_key': 'A', 'payload': 2}]\n    result = alpha_normalize_idempotency_keys(actions)\n    assert result == ({'idempotency_key': 123, 'payload': 1}, {'idempotency_key': 'K0', 'payload': 2})\n\ndef test_duplicate_non_string_keys():\n    actions = [{'idempotency_key': 123, 'payload': 1}, {'idempotency_key': 123, 'payload': 2}]\n    result = alpha_normalize_idempotency_keys(actions)\n    assert result == ({'idempotency_key': 123, 'payload': 1}, {'idempotency_key': 123, 'payload': 2})\n\ndef test_unicode_keys():\n    actions = [{'idempotency_key': '\u03b1', 'payload': 1}, {'idempotency_key': '\u03b2', 'payload': 2}]\n    result = alpha_normalize_idempotency_keys(actions)\n    assert result == ({'idempotency_key': 'K0', 'payload': 1}, {'idempotency_key': 'K1', 'payload': 2})\n\ndef test_no_modification_of_original():\n    original = [{'idempotency_key': 'A', 'payload': 1}]\n    result = alpha_normalize_idempotency_keys(original)\n    assert original[0]['idempotency_key'] == 'A'\n    assert result[0]['idempotency_key'] == 'K0'\n```", "rejected": "```python\nimport pytest\nfrom solution import alpha_normalize_idempotency_keys\n\ndef test_empty_sequence():\n    result = alpha_normalize_idempotency_keys([])\n    assert result == ()\n\ndef test_single_action_no_key():\n    result = alpha_normalize_idempotency_keys([{'other': 'value'}])\n    assert result == ({'other': 'value'},)\n\ndef test_single_action_with_key():\n    result = alpha_normalize_idempotency_keys([{'idempotency_key': 'A', 'other': 'value'}])\n    assert result == ({'idempotency_key': 'K0', 'other': 'value'},)\n\ndef test_multiple_actions_same_key():\n    result = alpha_normalize_idempotency_keys([{'idempotency_key': 'A', 'other': 'val1'}, {'idempotency_key': 'A', 'other': 'val2'}])\n    assert result == (({'idempotency_key': 'K0', 'other': 'val1'},), ({'idempotency_key': 'K0', 'other': 'val2'},))\n\ndef test_multiple_actions_different_keys():\n    result = alpha_normalize_idempotency_keys([{'idempotency_key': 'A', 'other': 'val1'}, {'idempotency_key': 'B', 'other': 'val2'}, {'idempotency_key': 'C', 'other': 'val3'}])\n    assert result == (({'idempotency_key': 'K0', 'other': 'val1'},), ({'idempotency_key': 'K1', 'other': 'val2'},), ({'idempotency_key': 'K2', 'other': 'val3'},))\n\ndef test_key_is_not_string():\n    result = alpha_normalize_idempotency_keys([{'idempotency_key': 123, 'other': 'val'}, {'idempotency_key': None, 'other': 'val2'}])\n    assert result == (({'idempotency_key': 123, 'other': 'val'},), ({'idempotency_key': None, 'other': 'val2'},))\n\ndef test_idempotency_key_changes_type_after_first_occurrence():\n    result = alpha_normalize_idempotency_keys([{'idempotency_key': 'first', 'other': 'a'}, {'idempotency_key': 1, 'other': 'b'}])\n    assert result[0]['idempotency_key'] == 'K0'\n    assert result[1]['idempotency_key'] == 1\n\ndef test_unicode_keys_get_canonicalized():\n    result = alpha_normalize_idempotency_keys([{'idempotency_key': '\u00e9moji', 'other': 'a'}, {'idempotency_key': '\u00e9moji', 'other': 'b'}])\n    assert result[0]['idempotency_key'] == 'K0'\n    assert result[1]['idempotency_key'] == 'K0'\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nimport json\n\n\ndef strict_json_compliant(text: str, allowed_actions: tuple[str, ...]) -> bool:\n    \"\"\"Check the prompt contract without the benchmark's recovery parser.\"\"\"\n    try:\n        payload = json.loads(text.strip())\n    except (json.JSONDecodeError, TypeError):\n        return False\n    if not isinstance(payload, dict) or payload.get(\"action\") not in allowed_actions:\n        return False\n    try:\n        confidence = float(payload[\"confidence\"])\n    except (KeyError, TypeError, ValueError):\n        return False\n    return 0.0 <= confidence <= 1.0 and isinstance(payload.get(\"brief_reason\"), str)\n\n```", "chosen": "```python\nimport json\nimport pytest\nfrom solution import strict_json_compliant\n\ndef test_valid_json_with_allowed_action_and_valid_confidence():\n    text = '{\"action\": \"type\", \"confidence\": 0.9, \"brief_reason\": \"test\"}'\n    assert strict_json_compliant(text, ('type', 'select')) is True\n\ndef test_valid_json_with_unallowed_action():\n    text = '{\"action\": \"invalid\", \"confidence\": 0.9, \"brief_reason\": \"test\"}'\n    assert strict_json_compliant(text, ('type', 'select')) is False\n\ndef test_invalid_json_syntax():\n    text = '{invalid json}'\n    assert strict_json_compliant(text, ('type', 'select')) is False\n\ndef test_missing_action_field():\n    text = '{\"confidence\": 0.9, \"brief_reason\": \"test\"}'\n    assert strict_json_compliant(text, ('type', 'select')) is False\n\ndef test_missing_confidence_field():\n    text = '{\"action\": \"type\", \"brief_reason\": \"test\"}'\n    assert strict_json_compliant(text, ('type', 'select')) is False\n\ndef test_confidence_out_of_range_low():\n    text = '{\"action\": \"type\", \"confidence\": -0.1, \"brief_reason\": \"test\"}'\n    assert strict_json_compliant(text, ('type', 'select')) is False\n\ndef test_confidence_out_of_range_high():\n    text = '{\"action\": \"type\", \"confidence\": 1.1, \"brief_reason\": \"test\"}'\n    assert strict_json_compliant(text, ('type', 'select')) is False\n\ndef test_confidence_not_a_number():\n    text = '{\"action\": \"type\", \"confidence\": \"not_a_number\", \"brief_reason\": \"test\"}'\n    assert strict_json_compliant(text, ('type', 'select')) is False\n```", "rejected": "```python\nimport pytest\nfrom solution import strict_json_compliant\n\ndef test_valid_json_compliant():\n    \"\"\"Valid JSON with allowed action, valid confidence, and brief_reason.\"\"\"\n    text = json.dumps({'action': 'list', 'confidence': 0.5, 'brief_reason': 'Good enough'})\n    assert strict_json_compliant(text, ('list',)) is True\n\ndef test_invalid_json():\n    \"\"\"Invalid JSON string.\"\"\"\n    assert strict_json_compliant('not json', ('list',)) is False\n    assert strict_json_compliant('', ('list',)) is False\n    assert strict_json_compliant('   ', ('list',)) is False\n\ndef test_invalid_action():\n    \"\"\"Valid JSON but action not in allowed_actions.\"\"\"\n    text = json.dumps({'action': 'delete', 'confidence': 0.5, 'brief_reason': 'Reason'})\n    assert strict_json_compliant(text, ('list',)) is False\n    assert strict_json_compliant(text, ('delete',)) is True\n\ndef test_invalid_confidence_type():\n    \"\"\"Confidence is not a number.\"\"\"\n    text = json.dumps({'action': 'list', 'confidence': '0.5', 'brief_reason': 'Reason'})\n    assert strict_json_compliant(text, ('list',)) is False\n    assert strict_json_compliant(json.dumps({'action': 'list', 'confidence': 0.5, 'brief_reason': 'Reason'}), ('list',)) is True\n\ndef test_confidence_out_of_range():\n    \"\"\"Confidence outside [0.0, 1.0] range.\"\"\"\n    text = json.dumps({'action': 'list', 'confidence': 1.5, 'brief_reason': 'Reason'})\n    assert strict_json_compliant(text, ('list',)) is False\n    text = json.dumps({'action': 'list', 'confidence': -0.1, 'brief_reason': 'Reason'})\n    assert strict_json_compliant(text, ('list',)) is False\n\ndef test_missing_brief_reason():\n    \"\"\"Valid JSON but missing brief_reason.\"\"\"\n    text = json.dumps({'action': 'list', 'confidence': 0.5})\n    assert strict_json_compliant(text, ('list',)) is False\n\ndef test_brief_reason_not_string():\n    \"\"\"Valid JSON but brief_reason is not a string.\"\"\"\n    text = json.dumps({'action': 'list', 'confidence': 0.5, 'brief_reason': 123})\n    assert strict_json_compliant(text, ('list',)) is False\n\ndef test_confidence_is_float():\n    \"\"\"Ensure float values work, including edge cases.\"\"\"\n    text = json.dumps({'action': 'list', 'confidence': 0.0, 'brief_reason': 'Zero confidence'})\n    assert strict_json_compliant(text, ('list',)) is True\n    text = json.dumps({'action': 'list', 'confidence': 1.0, 'brief_reason': 'Maximum confidence'})\n    assert strict_json_compliant(text, ('list',)) is True\n```"}
{"system": "You are an expert Python engineer writing pytest unit tests.\nRules:\n- The function under test is in a module named `solution`; import it with `from solution import <name>`.\n- Use plain pytest: functions named `test_*`, `assert`, and `pytest.raises` for errors. No fixtures, no mocks, no I/O, no network, no randomness.\n- Every test must pass on the implementation shown.\n- Aim to expose bugs: cover boundaries, empty and degenerate inputs, both sides of every condition, and error paths. Assert exact values, not just truthiness.\n- Write at most 8 test functions.\n- Reply with one ```python code block containing the complete test module and nothing else.", "prompt": "Write the pytest test module for this function.\n\n```python\nimport json\n\n\ndef strict_json_compliant(text: str, allowed_actions: tuple[str, ...]) -> bool:\n    \"\"\"Check the prompt contract without the benchmark's recovery parser.\"\"\"\n    try:\n        payload = json.loads(text.strip())\n    except (json.JSONDecodeError, TypeError):\n        return False\n    if not isinstance(payload, dict) or payload.get(\"action\") not in allowed_actions:\n        return False\n    try:\n        confidence = float(payload[\"confidence\"])\n    except (KeyError, TypeError, ValueError):\n        return False\n    return 0.0 <= confidence <= 1.0 and isinstance(payload.get(\"brief_reason\"), str)\n\n```", "chosen": "```python\nimport json\nimport pytest\nfrom solution import strict_json_compliant\n\ndef test_valid_payload_with_allowed_action():\n    \"\"\"Test valid payload with confidence and brief_reason.\"\"\"\n    text = json.dumps({'action': 'search', 'confidence': 0.8, 'brief_reason': 'fast'})\n    assert strict_json_compliant(text, ('search', 'query')) is True\n\ndef test_valid_payload_with_multiple_allowed_actions():\n    \"\"\"Test valid payload when action is in multiple allowed options.\"\"\"\n    text = json.dumps({'action': 'query', 'confidence': 0.5, 'brief_reason': 'simple'})\n    assert strict_json_compliant(text, ('search', 'query')) is True\n\ndef test_invalid_json_string():\n    \"\"\"Test invalid JSON string returns False.\"\"\"\n    assert strict_json_compliant('not json', ('action',)) is False\n\ndef test_missing_action_field():\n    \"\"\"Test missing 'action' field returns False.\"\"\"\n    text = json.dumps({'confidence': 0.8, 'brief_reason': 'test'})\n    assert strict_json_compliant(text, ('action',)) is False\n\ndef test_invalid_action_not_allowed():\n    \"\"\"Test action not in allowed_actions returns False.\"\"\"\n    text = json.dumps({'action': 'invalid', 'confidence': 0.8, 'brief_reason': 'test'})\n    assert strict_json_compliant(text, ('valid',)) is False\n\ndef test_confidence_out_of_range():\n    \"\"\"Test confidence outside 0.0 to 1.0 returns False.\"\"\"\n    text = json.dumps({'action': 'search', 'confidence': -0.1, 'brief_reason': 'test'})\n    assert strict_json_compliant(text, ('search',)) is False\n    text = json.dumps({'action': 'search', 'confidence': 1.1, 'brief_reason': 'test'})\n    assert strict_json_compliant(text, ('search',)) is False\n\ndef test_confidence_not_a_number():\n    \"\"\"Test confidence is not a number returns False.\"\"\"\n    text = json.dumps({'action': 'search', 'confidence': 'invalid', 'brief_reason': 'test'})\n    assert strict_json_compliant(text, ('search',)) is False\n\ndef test_missing_brief_reason():\n    \"\"\"Test missing 'brief_reason' is not a string returns False.\"\"\"\n    text = json.dumps({'action': 'search', 'confidence': 0.8})\n    assert strict_json_compliant(text, ('search',)) is False\n```", "rejected": "```python\nfrom solution import strict_json_compliant\n\ndef test_valid_json_with_allowed_action():\n    assert strict_json_compliant('{\"action\": \"fetch\", \"confidence\": 0.9, \"brief_reason\": \"fast\"}', ('fetch',)) is True\n\ndef test_invalid_json():\n    assert strict_json_compliant('not json', ('fetch',)) is False\n\ndef test_missing_action():\n    assert strict_json_compliant('{\"confidence\": 0.9, \"brief_reason\": \"test\"}', ('fetch',)) is False\n\ndef test_invalid_action():\n    assert strict_json_compliant('{\"action\": \"invalid\", \"confidence\": 0.9, \"brief_reason\": \"test\"}', ('fetch',)) is False\n\ndef test_confidence_out_of_range():\n    assert strict_json_compliant('{\"action\": \"fetch\", \"confidence\": -0.1, \"brief_reason\": \"test\"}', ('fetch',)) is False\n\ndef test_confidence_not_number():\n    assert strict_json_compliant('{\"action\": \"fetch\", \"confidence\": \"0.9\", \"brief_reason\": \"test\"}', ('fetch',)) is False\n\ndef test_missing_brief_reason():\n    assert strict_json_compliant('{\"action\": \"fetch\", \"confidence\": 0.5}', ('fetch',)) is False\n\ndef test_brief_reason_not_string():\n    assert strict_json_compliant('{\"action\": \"fetch\", \"confidence\": 0.5, \"brief_reason\": 123}', ('fetch',)) is False\n```"}
